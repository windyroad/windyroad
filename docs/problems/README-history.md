# Problem README: last-reviewed history

Forward-chronological archive of `docs/problems/README.md` line-3 fragments rotated out by the **Last-reviewed line discipline (P134)**. Newest fragment goes at the bottom under a date heading.

## 2026-05-02

> Last reviewed: 2026-04-27 (P032 verifying, levers 1+3 of the verify-first discipline fix shipped: memory note + CLAUDE.md addition; lever 2 hook deferred; review pass: P033 opened (report-upstream SKILL.md Step 5 example uses --label flag that fails when upstream repo hasn't pre-created the label; recommended fix is to drop the flag entirely since problem-report.yml frontmatter auto-applies labels on form submit; WSJF 12.0 Open S); P012 reported upstream as windyroad/agent-plugins#86 (git-push-gate.sh CI-status check missing) per /wr-itil:report-upstream Step 7 back-write; P028 enriched with batch-redundancy evidence (## Additional Evidence 2026-04-27 section: 8 sequential commits each fired a fresh wr-risk-scorer:pipeline subagent and all returned identical 1/25 Very Low verdicts on isomorphic single-line ## Reported Upstream appends; suggests shape-recognition caching as a candidate fix). All 18 dev-work tickets re-verified against on-disk inventory; 11 verifying tickets unchanged (none yet at the 14-day Likely-verified threshold); 0 parked. WSJF rankings stable since the prior pass except for the new P033 row inserted at the WSJF 12.0 cluster. P057 staging-trap signal observed in this session but not local-ticketed: upstream P057 covers the same root cause (manage-problem Step 7 staging trap, fix landed inline as the Edit-tool re-stage rule); decision logged in this iteration's retro per Task 4 default.

> Last reviewed: 2026-05-02 README reconciled (12 drift entries): added P034..P044 to WSJF Rankings, reordered Closed before Parked to match reconcile-readme.sh section-order assumption (false-positive STALE on P031 cleared). Reconciliation contract per P118 + ADR-014.

> Last reviewed: 2026-05-02 P026 re-rated (Effort S to XL, WSJF 32.0 to 4.0): fix-strategy correction supersedes ADR 021; new strategy requires building wr-itil:update-stale-deps skill upstream in wr-itil plugin per Step 7 pre-flight discipline. Re-rate audit-trail captured in ticket Effort line HTML comment.

> Last reviewed: 2026-05-02 P045 opened (assistant accepts ticket Fix Strategy upstream-placement framing without questioning), Open M WSJF 6.0; P026 re-rated to TBD (placement correction: skill is windyroad-local not upstream wr-itil per Tom mid AFK iter 2; previous 4.0/XL invalidated by upstream-framing reversal). Composes-with for P045 set to P032.

> Last reviewed: 2026-05-02 P035 transitioned Open to Verifying (newsletter drafter source-article quantitative-claim fidelity sub-section added to wr-newsletter SKILL.md step 11 as interim defence-in-depth; ADR-024 owns the structural fix). Earlier this session: P046 opened (risk scorer treats changesets as queued when underlying commits are already on origin), Open M WSJF 6.0. Live example surfaced this session when scorer returned release=8 above appetite for changesets whose introducing commits were already pushed to master.

## 2026-05-07

> Last reviewed: 2026-05-07 Pre-prep newsletter cleanup: P038 + P040 + P041 fix released as one SKILL.md commit per ADR-014 batch-grain (all three amend step 16; ADR-026 spec). Then P036 fix released as a separate SKILL.md commit (interim editorial-meta rule in step 11 voice rules; reassessment trigger after 4 editions; JTBD carve-outs preserve evidence-stance labels and Source-line attribution per JTBD review). All four tickets now in Verification Queue; verification triggers on tonight's prep run + tomorrow's finalise. Earlier this session: P026 closed as misframed (gate is intentional discipline, "like brushing teeth"); P020 cron verified live; AFK outstanding-questions queue cleared.

## 2026-05-10

> Last reviewed: 2026-05-10 README reconciled (2 drift entries corrected): P050 and P051 added to WSJF Rankings as deferred-WSJF placeholders (3.0 each, re-rate at next /wr-itil:review-problems). Both captured in-session via /wr-itil:capture-problem (P050 scope-creep on user corrections; P051 tier-1 source list missing US regulatory-news outlets). Reconciliation contract per P118 + ADR-014.

## 2026-05-12

> Last reviewed: 2026-05-11 P044 fix shipped on local master (release pending). Templated cover-image skill `.claude/skills/wr-newsletter-cover/SKILL.md` + parameterised template `assets/cover-template.svg` (placeholders for title, desc, wordmark, subtitle, two-line hook, week-ending) + render script `scripts/render-cover.mjs` (substitutes then shells out to `render-svg.mjs` for sips PNG) + vitest unit test (4 passing) landed. `/wr-newsletter` step 12 collapsed from 5-stage inline prose (12.a-e, ~45 lines) to single skill-invocation (~15 lines); step 12-prime delegates re-render on finalise headline change. Architect APPROVE-WITH-MODIFICATIONS (defer Playwright per ADR-011 no-binding to render pipeline; rename ticket's suggested `wr-newsletter:create-cover` to project-local `wr-newsletter-cover` since ADR-011 reserves colon-namespacing for marketplace plugins). End-to-end verification render of 2026-05-01 leader-edition inputs produced visually identical PNG to published `2026-05-01.cover.png`. Transitioned Open to Known Error (verifies on next prep+finalise cycle producing the cover in 1 invocation with no brand-mark, monogram, or font-weight reverts). Supersedes P037's in-line amendment; P037 should be marked superseded-by-P044 on next review. 2026-05-11 P037 fix shipped on local master (release pending). Step 12 of `.claude/skills/wr-newsletter/SKILL.md` rewritten into five labelled sub-steps (12.a mandatory brand-asset read with named canonical paths; 12.b typography conventions encoded from the 2026-05-01 published cover; 12.c sips silent-substitution traps and diagnostic mitigation; 12.d compose-the-cover; 12.e P011 render-and-verify). Minimum-viable hardening that addresses both documented root causes (soft brand-asset discovery, font-rendering silent-fallback) without absorbing P044's M-effort templated-skill scope. Architect PASS (no new ADR; refines ADR-017 confirmation criterion 5 with named paths). JTBD PASS (strengthens JTBD-001/002/003 visual-identity surface for the Engineering Leader persona). Transitioned Open to Known Error (verifies on next finalise producing the cover in <5 iteration rounds with no brand-mark, monogram, or font-weight reverts). 2026-05-11 P034 fix shipped on local master (release pending). Step 11.5 URL verification gate implemented in `.claude/skills/wr-newsletter/SKILL.md` per ADR-024 Decision Outcome: scope + Playwright/curl/DuckDuckGo transport selection + fresh-context `general-purpose` semantic subagent (SUPPORTED / REFUTED / NOT MENTIONED) + save-gate semantics + phase variant `11.5-prime` + audit-trail table in `.reviews.md` under `## URL Verification` per ADR-026; step 16 save templates updated (seven review blocks); step 17 surfaces per-URL verdict headline per criterion 4; failure modes cover REFUTED-unresolvable, fetch failure, multi-URL NOT MENTIONED. Architect ALIGNED, JTBD PASS (JTBD-200/203/205/001/003). Transitioned Open to Known Error (verifies on next prep + finalise cycle without user URL intervention + edition 4 zero-invented-URL spot-check). 2026-05-11 P039 fix shipped on local master (release pending). PASS_WITH_AUTHOR_OVERRIDES verdict variant added to `.claude/agents/wr-sw-critic.md` per ADR 025; caller plumbing landed in `.claude/skills/wr-newsletter/SKILL.md` (steps 9, 15, 15.25, 17, Failure modes); transitioned Open to Known Error (verifying on next push:watch + the following weekly newsletter edition). 2026-05-11 P006 transitioned to Verification Pending. OG share image fix shipped 2026-04-15 in commits `754a04a` (short-term: SVG patch-fitness headline + team-voice attribution removal) + `100df63` (permanent: shared `src/lib/siteCopy.mjs` constants with `src/lib/siteCopy.test.ts` reproduction test) on `origin/master`; awaiting production deploy to https://windyroad.com.au + LinkedIn Post Inspector cache flush + visual confirmation on next interactive session. 2026-05-11 P002 transitioned to Verification Pending. Hero fold-fit SCSS fix shipped 2026-04-25 in commit `72c3c2b` on `origin/master` (min-height 100svh, padding-bottom rebalance, short-viewport guard at max-height 600px); browser verification queued for next interactive session (1080p desktop, mobile, 200% / 400% zoom). 2026-05-11 P012 (CI-status check on push/release) shipped via `scripts/ci-status-check.sh` and ADR-028; transitioned to Verification Pending (verifies on next push:watch or release:watch attempt against red CI). Prior context (2026-05-10): backlog re-rated against new RISK-POLICY.md Impact rubric per ADR 027 (newsletter primacy encoded in rubric, folklore "weight 2.0" annotations dropped, P002 Known-Error multiplier corrected). 4 deferred placeholders scored (P050, P051, P052, P053). Top of queue (post-P034): P010 (9.0, source fetch KE), P024 (8.0, .claude/** permission gate).
> Last reviewed: 2026-05-12 **README reconciled** (1 drift entry corrected): P054 (work-problems Step 1 ranking does not exclude just-worked Known Error tickets awaiting orchestrator-owned push and transition) added to WSJF Rankings as a deferred-WSJF placeholder (1.5, Severity 3, Effort M; re-rate at next /wr-itil:review-problems). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").
> Last reviewed: 2026-05-12 P024 (.claude/** Edit/Write permission gate not satisfied by */Edit allow list in AFK subprocesses) transitioned Known Error to Verification Pending per ADR-022. Empirical PASS for `.claude/skills/**` colon-form matcher across 2026-05-11 AFK iters 3 to 6 (commits `fcd87e4`, `4dd2485`, `586c21e`, `95cd9c6` edited `.claude/skills/wr-newsletter/*` paths without python3 heredoc fallback). `.claude/agents/**` subtree verification queued behind a future intentional agent-file edit (architect-mandated A/B control surface). README WSJF Rankings row moved to Verification Queue with the agents-subtree caveat in Fix summary. Iter 1 of 2026-05-12 AFK loop completed manually after the dispatched subprocess hit a stream idle timeout API error mid-work and left a partial staged rename plus the Verification Outcome section in the working tree.
> Last reviewed: 2026-05-12 P043 (/wr-newsletter three-lens scoring lets non-leader-actionable items through) transitioned Open to Known Error per Option C user-direction (2026-05-11): each T / O / H lens now requires a `named_action` for the `<target-reader>` this quarter drawn from a persona-conditional action vocabulary (leader: procurement, capability investment, risk audit, etc.; developer: spot-test, adopt, pilot, watch, skip, abstain, upward-influence per JTBD-203, etc.). `.claude/skills/wr-newsletter/assets/three-lens-filter.md` rewritten (asset carries the full policy with vocabulary table); `.claude/skills/wr-newsletter/SKILL.md` step 4 bullet 2 amended with the in-flow named_action enforcement pointer. Third-tier prioritisation rewritten to use lens-level gating instead of the prior leader-persona post-filter (P016 no-map-anchor safety net preserved). AFK iter 2 dispatched twice; both attempts hit stream-idle-timeout API errors (first $unknown OS-suspend gap; second 32 min wall-clock $6.82 partial-edit-only). Orchestrator main turn completed SKILL.md edit, ticket transition, README refresh, and commit per ADR-013 Rule 1 interactive recovery. iter-2-retry JSON envelope preserved at `.afk-run-state/resolved/`.

> Last reviewed: 2026-05-12 Batch transition (5 tickets) Known Error to Verification Pending per ADR-022 after push:watch drained 12 commits to `origin/master`: P034 (URL verification gate, commit `4dd2485`), P037 (cover-image step rewrite, `586c21e`), P039 (PASS_WITH_AUTHOR_OVERRIDES verdict variant, `fcd87e4`), P043 (three-lens named_action lens-tightening, `219377b`), P044 (templated cover-image skill, `95cd9c6`). All 5 ride the same /wr-itil:transition-problems batch commit per ADR-014 batch grain. P024 remains in Verification Pending with the `.claude/agents/**` A/B carve-out (transitioned earlier today, commit `4ffccac`). Iter 2 of today's AFK loop hit stream-idle-timeout API errors twice; orchestrator main turn completed the iter-2 work + the batch transition manually per ADR-013 Rule 1 interactive recovery. iter-1 + iter-2-retry JSON envelopes preserved at `.afk-run-state/resolved/`.

> Last reviewed: 2026-05-12 **README reconciled** (1 drift entry corrected): P055 (AFK iter subprocess `Stream idle timeout` API errors recur at high rate) added to WSJF Rankings as a deferred-WSJF placeholder (1.5, Severity 3, Effort M; re-rate at next /wr-itil:review-problems). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").

> Last reviewed: 2026-05-12 P010 (source-tier fetch blocked by OpenAI bot protection + Reddit tool-layer refusal) re-rated 9.0 to 4.5 via P076 transitive-effort rule. Added formal `## Dependencies` block to P010 (Blocked by P014) and mirror `Blocks: P010` to P014. Effort propagates marginal M to transitive L through the P014 dependency since the Reddit half of P010 has no in-pipeline workaround absent P014's Playwright helper. Honest WSJF prevents P010 out-ranking its blocker. Ticket order at the 4.5 tier: P010 (Known Error, L) before P050 (Open, M) per the canonical tie-break ladder.

> Last reviewed: 2026-05-12 Iter 3 review pass: no Priority/Effort/WSJF changes versus iter 1. Iter 2 (commit `31ffd77`) appended the upstream-pending marker to P042 (jtbd-enforce-edit hook relative-path bug), joining P021/P028/P033 with formal `## Reported Upstream` sections. The entire 8.0 tier now carries upstream markers and is classified upstream-blocked by /wr-itil:work-problems for non-interactive AFK iters. Effective top-of-queue for AFK runs: 6.0 tier. P014 (Playwright helper, Known Error, L) leads per the Known-Error-first rung, then P022/P027/P047/P049 (Open, S) by Effort-asc + Reported-asc tie-break ladder, then P046/P051/P052/P053 (Open, M). Verification Queue surfaces 24 entries; AFK fallback per Step 4 records the queue without firing AskUserQuestion (P130 transient user).

> Last reviewed: 2026-05-12 P050 (assistant scope-creeps on user-flagged corrections, changes more than asked) transitioned Open to Verification Pending per ADR-022. Lever 1 of the three-lever fix shipped: new feedback memory note `feedback_smallest_change_satisfies_correction.md` added to the `~/.claude/projects/-Users-tomhoward-Projects-windyroad/memory/` tree directing the assistant, on correction signals (FFS, "no don't", "I said X but you did Y", "FUCK", "wait", explicit reversal), to execute the smallest atomic change that satisfies the literal correction without redesigning adjacent surfaces. MEMORY.md index updated. Mirrors the three-lever pattern previously applied to P032 (assertion scope) and P045 (placement-claim propagation); the assistant-discipline family now covers P032 + P045 + P050. Levers 2 (CLAUDE.md amendment) and 3 (UserPromptSubmit hook) deferred per the ticket's default; reassessment trigger if scope-creep recurs past 3 sessions. Verification triggers on next correction-signal turn where the assistant executes literal-only and does not bundle adjacent redesigns. AFK iter 3 of 2026-05-12 loop (parent of iter-3 review-pass commit `e778fe0`).

> Last reviewed: 2026-05-12 P051 (Newsletter tier-1 source list misses US regulatory-news outlets) transitioned Open to Verification Pending per ADR-022. AFK iter 5 of 2026-05-12 work-problems loop. Shipped: `.claude/skills/wr-newsletter/SKILL.md` step 2 Tier 1 block now carries a `US/UK AI-policy news (via Google News RSS, P051)` entry combining Politico + Axios + Reuters into a single Google News RSS scoped query (mirrors the OpenAI P010 workaround shape; halves map-update-block surface vs three separate queries per JTBD review feedback). Tier 2 block carries the analogous NYT + Bloomberg + WSJ entry. Three stale inline tier-1 enumerations at SKILL.md lines 131/182/219 replaced with back-references to the tier-1 block (per architect first-pass ISSUES FOUND, durable). ADR-017 Constraints text amended to reflect the expanded tier-1 set and opportunistically fix the pre-existing "OpenAI via Playwright workaround" to "OpenAI via Google News RSS workaround per P010" drift. Architect PASS (after revisions); JTBD PASS (serves JTBD-001/002/003 for the Engineering Leader persona). Verification triggers on next prep cycle where the new combined query surfaces an AI-policy story the previous list would not have caught (e.g. the 2026-05-08 CAISI / pre-release-vetting story shape). Deferred to follow-up: three-lens-filter.md primary-outlet allowlist update; standalone source-tier-policy ADR.

## 2026-05-13

> Last reviewed: 2026-05-12 P045 (Assistant accepts ticket Fix Strategy upstream-placement framing without questioning) transitioned Open to Verification Pending per ADR-022. AFK iter 6 of 2026-05-12 work-problems loop. Lever 1 completed: memory file `feedback_verify_upstream_placement_before_propagating.md` shipped 2026-05-02; CLAUDE.md "Verify before asserting" extension shipped this iter as a second paragraph sibling to the existing P032 note, naming the trigger (Fix Strategy upstream-placement claim), the two verification questions (domain fit, placement authority), and the parroting anti-pattern. Architect ALIGNED (no new ADR; sibling-shape extension). JTBD PASS (internal note). Levers 2 and 3 deferred per the ticket default. Verification triggers on the next iteration that processes upstream-placement prose.

> Last reviewed: 2026-05-13 Review pass (pragmatic, scoped to fold new captures). P056 (manage-problem ADR-014 misattribution), P057 (no-em-dash.sh Edit-tool whitelist port), and P058 (architect+JTBD retros exclusion) added to WSJF Rankings as deferred-WSJF placeholders (P056: 1.5, Severity 3, Effort M; P057: 3.0, Severity 3, Effort S; P058: 3.0, Severity 3, Effort S). Full per-ticket re-rate deferred (23 dev-work tickets, requires deep read per ticket). 28-ticket Verification Queue prompt skipped per ADR-013 Rule 6 fail-safe at end-of-loop wall-clock; queue recorded as-is for next interactive session. No auto-transitions Open to Known Error fired this pass. Reconciliation contract per P118 + ADR-014 amended.

> Last reviewed: 2026-05-13 **README reconciled** (1 drift entry corrected): P059 (`npm run push:watch` exits 141 (SIGPIPE) on successful push) added to WSJF Rankings as a deferred-WSJF placeholder (1.5, Severity 3, Effort M; re-rate at next /wr-itil:review-problems). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").

> Last reviewed: 2026-05-13 P014 (Playwright helper for tool-blocked sources) Known Error to Verification Pending per ADR-022. All four phases shipped this session: P014a OpenAI walking skeleton (commit `530171f`), P014b Reddit coverage via `shreddit-post` HTML scrape (commit `c2aa2db`), P014c SKILL step 2 auto-invoke rung, P014d `validateCacheEntry` JSON schema validation + retry-once-on-zero-items (this commit). 33 vitest cases passing. ADR-029 captures cache layout + 48h freshness + three-rung precedence. Unblocks P010.

> Last reviewed: 2026-05-13 P053 (/wr-newsletter formal gate sequence missing cog-a11y) Open to Verification Pending per ADR-022. SKILL step 15.4 inserted between editor (15.25) and LinkedIn-draft (15.5) per the P053 default proposal confirmed 2026-05-13: invokes `cognitive-accessibility` subagent on the brief body, one-round-with-optional-remediation pass, phase variant 15.4-prime mirrors 15.25-prime. Step 16 save templates updated for both phases. Skip-on-upstream-REJECTED honoured.

> Last reviewed: 2026-05-13 P059 (`npm run push:watch` exits 141 SIGPIPE) Open to Verification Pending per ADR-022. `scripts/push-watch.sh` 7 pipe-to-head chains (lines 71, 75, 191, 213, 225, 243, 284) now append `|| true` inside the command substitution; pipefail-induced 141 from SIGPIPE is neutralized. Each chain captures into a variable; downstream callers already handle empty/zero degraded-failure cases. ADR-021 "Robustness shape" precedent. Architect PASS. Audit-trail correction: ticket initially named 5 chains; verification found 7.

## 2026-05-15

> Last reviewed: 2026-05-15 README reconciled. 9 newsletter / capture-adr / assistant-gate friction tickets captured from /wr-retrospective:run-retro session: P060 P061 P062 P063 P064 P065 P066 P067 P068. Reconciliation contract per P118 + ADR-014.

## 2026-05-16

> Last reviewed: 2026-05-16 P028 (risk-scorer 30-min TTL expired during long-running orchestrator turns) Open to Known Error per ADR-022. Root cause confirmed upstream in `@windyroad/risk-scorer` v0.9.0: three-band TTL policy (Band A age<TTL/2 passes silently; Band B TTL/2<=age<TTL slides marker forward on invariant state-hash, bounded by 2*TTL hard cap, CHANGELOG `43e9cc0`) plus subprocess-return slide mechanism (`slide-marker-on-subprocess-return.bats`). Default TTL also bumped 1800s to 3600s per upstream P107 symptom-treatment in an earlier release. Investigation tasks 1-5 all answered upstream; no local fix required. Verification will land on the Known Error to Verification Pending transition immediately following.

## 2026-05-25

> Last reviewed: 2026-05-16 P028 (risk-scorer 30-min TTL expired during long-running orchestrator turns) Known Error to Verification Pending per ADR-022. Fix released in upstream `@windyroad/risk-scorer` v0.9.0 (CHANGELOG `43e9cc0`); three-band TTL policy (Band A passes silently when age<TTL/2; Band B slides marker forward on invariant state-hash bounded by 2*TTL hard cap; Band C halts as before) plus `slide-marker-on-subprocess-return` mechanism shipped and installed locally for this project. Awaiting user verification on the next long AFK orchestrator turn where the staged-tree state-hash is invariant across the TTL window; expected behaviour: Band B slide admits the commit without an extra `wr-risk-scorer:pipeline` invocation.

## 2026-05-30

> Last reviewed: 2026-05-25 P070 opened (newsletter draft-template does not codify the three-deep-items-plus-notes editorial discipline). Captured by /wr-retrospective:run-retro after The Shift Issue 06 finalise. Priority 3 (Low), Effort S; exact WSJF deferred to the next /wr-itil:review-problems.
> Last reviewed: 2026-05-30 P061 (assistant gates policy-authorised actions on user permission when risk-scorer has already cleared) Open to Known Error per ADR-010 transition-problem skill; root-cause analysis identified the wrap-up regression manifests as deferral-prose ("Push decision deferred pending direction") not direct-pitch ("Should I push?"), evading the upstream `itil-assistant-output-review.sh` PROSE_ASK_PATTERNS detector which keys on direct phrasings. Memory-layer fix shipped: extended `feedback_no_pitching_act_on_obvious_decisions.md` (project-local user memory) with a new "Wrap-up surface for policy-authorised post-clearance actions" section enumerating four canonical deferral-prose phrasings, the post-risk-scorer-clearance trigger condition, the equivalence rule (treat deferral-prose as a pitch), and the 2026-05-14 source incident as anti-pattern witness. Upstream-blocked follow-ups recorded on ticket: PROSE_ASK_PATTERNS extension in `@windyroad/itil` `lib/detectors.sh` and `check-ask-hygiene.sh` measurement-layer extension in `@windyroad/retrospective`. No external-root-cause hit (memory layer is the in-project lever). Verification trigger: next AFK iter or interactive session wrap-up where every commit cleared within appetite and assistant pushes directly without deferral-prose. AFK iter 10.
> Last reviewed: 2026-05-30 P063 (newsletter cover `hook_line_1` budget too loose for LinkedIn preview crop) Open to Known Error per ADR-010 transition-problem skill; fix already shipped in commit `eb0e8be` (2026-05-16 publish of The Shift Issue 05) tightened `.claude/skills/wr-newsletter-cover/SKILL.md` hook_line_1 budget from "around 30 chars max" to "around 28 chars max" with LinkedIn-crop rationale inline after observing crop on a 30-char hook ("AI cyber capabilities shipped.") during that run. Current SKILL.md line 33 confirms the 28-char rule plus rationale referencing the witnessed crop; line 37 carries the safe-area preview-discipline note ("safe-area lives ~2 character-widths inside each margin"). Pre-flight: root cause documented (LinkedIn ~1200x627 preview crop from ~1200x630 source), workaround documented (keep hook_line_1 <= 28 chars), reproduction evidence captured. No external-root-cause hit. Transition verifies the shipped fix; verification trigger fires on next `/wr-newsletter-cover` invocation that produces a <= 28-char hook_line_1 with no LinkedIn preview crop. AFK iter 7.
> Last reviewed: 2026-05-30 P062 (newsletter persona-config edition-count rule globs sibling files) Open to Known Error per ADR-010 transition-problem skill; broken `<published>/*.md` glob in two persona configs and SKILL.md step 11 replaced with `max(frontmatter edition:)+1` across published+drafts, filename filter narrowed to canonical `YYYY-MM-DD.md` shape so ADR-026 sibling files (`.linkedin.md`, `.reviews.md`, `.capture.md`) and `README.md` are excluded by construction; SKILL.md step 11 also gained an assert-max+1 safeguard with surface-to-Tom-on-mismatch so future runs surface rather than publish wrong issue numbers. Architect PASS (no ADR conflict; ADR-026 sibling-file convention is the upstream change the broken glob failed to accommodate). JTBD PASS (defect repair to baseline reader-trust requirement for both newsletter jobs). AFK iter 6.
> Last reviewed: 2026-05-30 P057 (no-em-dash.sh Edit/Write hook lacks contractual-marker whitelist) Open to Verification Pending per ADR-022; WHITELIST_LINE check ported from `no-em-dash-bash.sh` to `no-em-dash.sh`; both hooks now carry a matching 3-entry whitelist (P063 upstream-pending marker + capture-problem deferred-placeholder Priority/Effort lines); bash hook also expanded to the 3-entry shape for surface symmetry per architect note. 6 inline smoke tests verify canonical markers pass and non-whitelisted em-dashes still block. Architect PASS, JTBD PASS. AFK iter 5.
> Last reviewed: 2026-05-30 P068 (newsletter Google News RSS strips canonical URL to outlet root) investigated; fix shape ticked (Playwright resolution, option a), AFK iter 4 paused before SKILL.md edit. Architect ISSUES FOUND (new ADR-031 codifying Google News redirect resolution as a pipeline primitive required before SKILL.md amendment lands; scope edit to rung 3 only at line 151 plus lines 154 / 178 / 225; decide whether new `scripts/resolve-gnews-urls.mjs` shares primitives with existing `scripts/playwright-fetch.mjs`). JTBD FAIL (seven unratified JTBDs and personas gate this work: engineering-leader persona plus JTBD-001 / 002 / 003, developer persona plus JTBD-203 / 205, all `.proposed.md` lacking `human-oversight: confirmed`). Classified `architect-design`, deferred to interactive cycle per ADR-044 and P135 AFK hygiene. Findings recorded on P068 ticket.
> Last reviewed: 2026-05-30 P066 (voice guide LinkedIn-posts section) Open to Verification Pending per ADR-022; three carve-out paragraphs added to `docs/VOICE-AND-TONE.md` § LinkedIn posts (auto-share link, recurring-newsletter cadence anchor, substantive content-tied question). JTBD PASS; voice-tone PASS round 2 after `actually` word-list fix on the carve-out example. AFK iter 3 work.
> Last reviewed: 2026-05-30 P010 (source-fetch blocks OpenAI and Reddit) Known Error to Verification Pending per ADR-022. P014 Playwright helper shipped 2026-05-13 across 4 commits (`530171f` P014a OpenAI walking skeleton, `c2aa2db` P014b Reddit `shreddit-post` scrape, plus P014c auto-invoke and P014d schema-validation+retry). Fix path exercised end-to-end in the 2026-05-25 published edition: tier-1 OpenAI surfaced via canonical `openai.com/index/advancing-content-provenance/` URL in `2026-05-25.capture.md`; tier-2 r/MachineLearning surfaced via canonical permalink in `2026-05-25.md` line 51. Composes-with P014 (same root cause, transitive verification).

> Last reviewed: 2026-05-30 batch re-rate per /wr-itil:review-problems: Origin field stamped on all 65 tickets per ADR-076 (no inbound channels configured; all internal); Priority/WSJF math corrected on 30 open/known-error tickets (Severity band labels normalised from "Significant"/"Moderate" to canonical High/Medium per RISK-POLICY label bands; deferred re-rate placeholders resolved); 7 tickets lacking Effort lines (P021, P022, P023, P027, P029, P030, P070) gained concrete Effort + WSJF; dependency-graph traversal found one active edge (P023 blocked by P021 + P022) with no transitive propagation needed. No auto-transitions Open to Known Error fired this pass: the eligible-by-criteria cluster is dominated by upstream-blocked tickets per P069, and promoting to KE with multiplier 2.0 would worsen the upstream cycling problem before the placement-authority signal lands. Verification Queue uses P186 evidence-first cells (only P010 carries observed evidence; remainder marked `no - not observed` pending verification triggers). Top of dev-work queue: P062 (18.0 KE S newsletter persona-config edition-count), P063 (16.0 KE S cover hook_line_1 budget), P021 (16.0 Open S architect-mark-reviewed strict-verdict).

> Last reviewed: 2026-05-30 README reconciled (1 drift entry corrected): P073 added to WSJF Rankings at WSJF=8 cluster. Reconciliation contract per P118 + ADR-014.

> Last reviewed: 2026-05-30 P062 (newsletter persona-config edition-count rule globs sibling files) Known Error to Verification Pending per ADR-022; fix released 2026-05-30 in commit `de6c165` (local-skill change in `.claude/skills/wr-newsletter/`, no changeset required). Static spot-check this iter: rule produces next leader edition = 7 from max(frontmatter `edition:` across `YYYY-MM-DD.md` matches) = 6 across published+drafts; old `*.md` glob would have returned ~11. Awaiting live verification on next `/wr-newsletter` run. AFK iter 1.

> Last reviewed: 2026-05-30 P063 (newsletter cover `hook_line_1` budget too loose for LinkedIn preview crop) Known Error to Verification Pending per ADR-022; fix released 2026-05-16 in commit `eb0e8be` (The Shift Issue 05 publish; `.claude/skills/wr-newsletter-cover/SKILL.md` hook_line_1 budget tightened from "around 30 chars max" to "around 28 chars max" with LinkedIn-crop rationale inline, plus safe-area preview-discipline note added at line 37). Local-skill change, no changeset required. Awaiting live verification on next `/wr-newsletter-cover` invocation producing a 28-char-or-shorter hook_line_1 with no LinkedIn preview crop. AFK iter 2.

> Last reviewed: 2026-05-30 P021 (architect-mark-reviewed.sh strict-verdict-string parsing under-counts affirmative ISSUES FOUND verdicts as FAIL) Open to Parked, upstream-blocked. Fix lives in the `wr-architect` plugin hook at `~/.claude/plugins/cache/windyroad/wr-architect/<version>/hooks/architect-mark-reviewed.sh`; a marketplace consumer cannot edit the cached hook without losing the change on next plugin update. Verified 2026-05-30: latest cached `0.12.2` still ships the strict `grep -q "Architecture Review: PASS"` parser at lines 27 to 30 (the `ISSUES FOUND` branch unconditionally routes to `VERDICT="FAIL"`). Upstream `windyroad/agent-plugins#78` is OPEN; upstream tracks it locally as P217 with the `safe-low-fix-risk` label. Un-park trigger: a new `wr-architect` plugin release whose parser recognises affirmative-bottom-line `ISSUES FOUND` verdicts as PASS. AFK iter 3.

> Last reviewed: 2026-05-30 P027 (work-problems Step 5 exit-code rule does not handle is_error:true transient API failures, 529 Overloaded) Open to Parked, upstream-blocked. Fix lives in the `wr-itil` plugin SKILL.md Step 5 at `~/.claude/plugins/cache/windyroad/wr-itil/<version>/skills/work-problems/SKILL.md`; a marketplace consumer cannot edit the cached SKILL.md without losing the change on next plugin update, and the orchestrator-side workaround is not codified locally either (dispatch wrapper is a transient `/tmp/wr-iter-N-dispatch.sh` generated per-iter by the SKILL.md). Verified 2026-05-30: latest cached `0.38.0` still ships the halt-on-no-staged-work branch at lines 485 to 490; the P261 amendment salvages only `is_error: true` WITH staged files, while the ELSE branch at line 488 halts on `is_error: true` with nothing staged, which is exactly the 529 Overloaded shape (`total_cost_usd: 0, num_turns: 1`). Upstream `windyroad/agent-plugins#81` is OPEN (last updated 2026-05-15 per `gh issue view 81`); tracked upstream as P214 (safe-low-fix-risk). Un-park trigger: a new `wr-itil` plugin release whose `work-problems/SKILL.md` Step 5 extends the salvage carve-out to retry-once on `is_error: true` with `total_cost_usd: 0` (or ships an alternative distinguishing transient API failures from work-completion failures). AFK iter 5.

> Last reviewed: 2026-05-30 P068 (Newsletter URL discovery via Google News RSS strips canonical to outlet root; misses real article URL) Open to Parked, architect-design (NOT upstream-blocked). Distinction matters: iters 3-9 parked tickets whose fix sites lived in marketplace plugin caches (genuinely unmovable from this project); P068's fix site IS in-project (`.claude/skills/wr-newsletter/SKILL.md`, `scripts/resolve-gnews-urls.mjs`, `scripts/playwright-fetch.mjs`). Block is internal-interactive, not external-blocked. Two pre-edit gate findings from prior iter 4 remain in force on iter 10 re-affirmation: (a) ADR-031 "Google News redirect resolution as a pipeline primitive" unauthored (`wr-architect-is-decision-unconfirmed ADR-031` would exit 2 = not found; new-ADR creation is the substance-confirm-before-build trigger per ADR-074); (b) seven unratified JTBDs/personas (`engineering-leader/persona.md` + `JTBD-001/002/003.proposed.md`, `developer/persona.md` + `JTBD-203/205.proposed.md`) all lack `human-oversight: confirmed`. Both blocks require interactive cycle (`/wr-architect:create-adr` + `/wr-jtbd:confirm-jobs-and-personas`); per P135 the AFK loop cannot fire `AskUserQuestion`. Park is hygiene-only: without it, every future AFK loop re-selects P068 first (WSJF=12 top of queue) and burns an iter slot on the same re-confirmation. Un-park trigger: ratify the seven JTBDs/personas via `/wr-jtbd:confirm-jobs-and-personas` AND author ADR-031 via `/wr-architect:create-adr`; after both, Parked to Open + restore WSJF=12 + execute the seven-step build plan from iter 4 (ADR-031, shared-primitives decision, generalise `resolve-gnews-urls.mjs`, vitest, SKILL.md amendment at lines 151/154/178/225, single-bundle commit, Open to Verifying). AFK iter 10.

> Last reviewed: 2026-05-30 P061 (assistant gates policy-authorised actions on user permission when risk-scorer has already cleared) Known Error to Verification Pending per ADR-022. Memory-layer fix released in commit `9f42130` (AFK iter 10, today): `feedback_no_pitching_act_on_obvious_decisions.md` (project-local user memory at `~/.claude/projects/-Users-tomhoward-Projects-windyroad/memory/`) carries a new "Wrap-up surface for policy-authorised post-clearance actions (P061)" section enumerating four canonical deferral-prose phrasings ("Push decision still deferred pending explicit user direction", "Awaiting clearance to push", "Holding off on push pending your call", "Push remains deferred pending direction"), the post-risk-scorer-clearance trigger condition, the equivalence rule (treat deferral-prose as a pitch; same forced round-trip outcome as a direct pitch, just grammatically inverted to evade the upstream `itil-assistant-output-review.sh` direct-pitch regex), and the 2026-05-14 source incident as anti-pattern witness. Project-local user-memory edit with no release pipeline (commit-as-release-marker per ADR-022 user-memory carve-out, same pattern as P062 / P063 / P066 project-local skill edits). Upstream-blocked follow-ups (extend `@windyroad/itil` `lib/detectors.sh` PROSE_ASK_PATTERNS to include deferral-prose shapes; extend `@windyroad/retrospective` `check-ask-hygiene.sh` to scan emitted output for both direct-pitch AND deferral-prose shapes) tracked separately on the ticket as proposals. Verification trigger: next AFK iter or interactive session wrap-up where risk-scorer has cleared every commit in chain within RISK-POLICY.md appetite AND the assistant performs the push directly without emitting any of the four canonical deferral-prose phrasings or naming the user as the gate. AFK iter 11.

> Last reviewed: 2026-05-30 P060 (`capture-adr` SKILL.md template frontmatter sentinel carries U+2014 that no-em-dash hook blocks) Open to Parked, upstream-blocked. Fix lives in the `wr-architect` plugin template at `~/.claude/plugins/cache/windyroad/wr-architect/<version>/skills/capture-adr/SKILL.md`; a marketplace consumer cannot edit the cached SKILL.md without losing the change on next plugin update. Local whitelist option (extend `.claude/hooks/no-em-dash*.sh` WHITELIST_LINE entries with the capture-adr template sentinels) is a band-aid that would orphan once upstream substitutes the U+2014 characters, and propagates whitelist debt across every adopter shipping the no-em-dash hook style. Verified 2026-05-30: latest cached `0.12.1` still ships the U+2014 template literals at four sites in `skills/capture-adr/SKILL.md` (lines 76, 96, 97, 129), all part of the template literal written verbatim into new `.proposed.md` ADR files. No upstream issue filed yet (three `gh issue list -R windyroad/agent-plugins --search` queries returned empty); standing "Upstream report pending" marker on the ticket. Un-park trigger: a new `wr-architect` plugin release whose `capture-adr` template substitutes the four U+2014 characters with commas, colons, or parentheses (canonical-expansion auto-detection string is already long-dash-free, no detection-contract drift). Local impact while parked: existing Workaround (pre-substitute U+2014 on second Write attempt, around 30 seconds per capture) remains the operating contract. AFK iter 12.

> Last reviewed: 2026-05-30 P073 (P186 evidence-first cell canonical shape uses U+2014 separator that no-em-dash hook blocks during README render) Open to Parked, upstream-blocked. Fix site lives in the upstream `wr-itil` plugin SKILL.md files (`skills/review-problems/SKILL.md` lines 78 to 82 defining the three canonical cell shapes plus four other render sites in `skills/manage-problem/`, `skills/reconcile-readme/`, `skills/transition-problem/`, `skills/transition-problems/`, `skills/list-problems/`, all keyed by the `<!-- LIKELY-VERIFIED-CELL-SHAPE: evidence-based per P186 -->` drift-tripwire marker). Marketplace consumer cannot edit the cached SKILL.md without losing the change on next plugin update. Local whitelist option (extend `.claude/hooks/no-em-dash*.sh` WHITELIST_LINE entries with the three P186 cell prefixes) is a band-aid that would orphan once upstream substitutes the U+2014 separators with parens / colons / ASCII hyphens, and propagates whitelist debt across every adopter shipping the no-em-dash hook style. Verified 2026-05-30: cached `wr-itil@0.34.0` ships the U+2014 separators live across all six render sites. No upstream issue filed yet; standing "Upstream report pending" marker on the ticket. Un-park trigger: a new `wr-itil` plugin release whose P186 cell-shape contract substitutes the U+2014 separators with ASCII alternatives across all six render sites (verify by re-reading the cached files, then re-running `/wr-itil:review-problems` and confirming the README Verification Queue write succeeds without bulk-python substitution). Local impact while parked: existing Workaround (bulk python substitution at README write time converting `yes [U+2014] observed: X` to `yes (observed: X)`, `no [U+2014] not observed` to `no (not observed)`, `no [U+2014] observed regression [U+2014] X` to `no (observed regression): X`) remains the operating contract, preserves reader-facing semantic meaning, diverges from P186 canonical wire format. Same shape as iters 3 to 12 (P021, P022, P027, P033, P042, P047, P049, P060) marketplace-consumer-cannot-edit-cached-plugin parks already merged today. README.md WSJF Rankings row removed; Parked-section row appended after P068 per ID sort. README-history.md rotated iter-12 P060 Open to Parked entry under 2026-05-30 heading per P134 last-reviewed-line discipline. AFK iter 13.

## 2026-05-31

> Last reviewed: 2026-05-31 P029 (work-problems iteration boundary leaves run-retro BRIEFING.md edits uncommitted) Open to Parked, upstream-blocked. Fix sites live in upstream `wr-itil` and `wr-retrospective` plugin SKILL.md files: `~/.claude/plugins/cache/windyroad/wr-itil/0.38.0/skills/work-problems/SKILL.md` Step 6 / 6.4 / 6.5 / 6.75 commit-staging contract (lines 394 + 506 + 525 + 564 + 681 + 713 covering retro-on-exit, progress reporting, risk-register drain, release-cadence check, inter-iteration verification) AND `~/.claude/plugins/cache/windyroad/wr-retrospective/0.21.4/skills/run-retro/SKILL.md` ownership boundary (line 441: "run-retro does not commit its own work"). Verified 2026-05-31: the two cached SKILL.md texts CONTRADICT each other on whether run-retro commits BRIEFING.md edits (work-problems line 394 says it does per upstream ADR-014; run-retro line 441 says it does not per the same ADR). The contract ambiguity itself is the upstream defect. Local "ADR-014 amendment" option proposed in the ticket's Investigation Tasks does NOT apply: this project's `docs/decisions/014-wardley-mapping-as-strategic-lens.proposed.md` is a Wardley-mapping decision, not the governance-skills-commit-own-work ADR (that one lives upstream). Marketplace consumer cannot edit cached SKILL.md without losing the change on next plugin update. Upstream `windyroad/agent-plugins#83` is OPEN (last updated 2026-05-15 per `gh issue view 83`). Un-park trigger: a new `wr-itil` plugin release that stages BRIEFING.md in the iteration commit before risk-scoring, OR a new `wr-retrospective` release that carves out work-problems-inside-subprocess for direct BRIEFING.md commit, OR upstream #83 closes with linked PR landing either fix. Local impact while parked: empirically ZERO. This project has no `docs/briefing/` tree and only a legacy `docs/BRIEFING.md` stub; all 13 prior AFK iters today reported `No briefing changes this iter` and produced zero forced hand-off commits (the ticket's cited iters 7 / 9 / 11 hand-off commits `4c644e5` / `3b3a703` / `556ef92` / `180b829` predate the current iter-prompt template's pre-stage discipline). Composes with P028 (verifying) as the TTL-expiry composition the ticket cites, but the composition is dormant in this project since the empirical-trigger frequency is currently nil. Park preserves Severity 12 (High) for upstream-tracking visibility. Same marketplace-consumer-cannot-edit-cached-plugin shape as iters 3 to 13 parks already merged this AFK loop. README.md WSJF Rankings row removed (was top-of-queue at WSJF 6); Parked-section row appended after P027 / before P031 per ID sort. README-history.md rotated iter-13 P073 Open to Parked entry under 2026-05-30 heading per P134 last-reviewed-line discipline. AFK iter 14.

> Last reviewed: 2026-05-31 P046 (Risk scorer treats changesets as queued when underlying commits are already on origin) Open to Parked, upstream-blocked. Fix site lives in upstream `wr-risk-scorer` plugin at `~/.claude/plugins/cache/windyroad/wr-risk-scorer/<version>/agents/pipeline.md` release-layer scoring guidance (or alternatively `skills/assess-release/SKILL.md` step 4 prompt-build); marketplace consumer cannot edit cached agent prose or SKILL.md without losing the change on next plugin update. Verified 2026-05-31: latest cached `0.11.2` still ships the bug. `skills/assess-release/SKILL.md` step 2 (lines 36 to 37) lists `.changeset/*.md` filenames via `ls .changeset/*.md` and step 4 (lines 59 to 62) passes "the changeset list (if any)" to the pipeline subagent without any `git merge-base --is-ancestor` push-state verification; `agents/pipeline.md` `grep -n` for `changeset|merge-base|--is-ancestor` returns 16 hits on `changeset` for graduation-from-holding logic (ADR-061 / ADR-042) and zero on the P046 push-state-verification rule. Option 2 caller-side defensive wrapper (re-verify `.changeset/*.md` ancestry in `wr-itil` orchestrator skills before acting on Layer 1 release-risk score) is itself upstream prose in `work-problems/SKILL.md` Step 6.5 and `manage-problem/SKILL.md` Step 12; same marketplace-consumer-cannot-edit-cached-plugin shape one plugin over. No local-codifiable surface. Upstream `windyroad/agent-plugins#121` is OPEN (last updated 2026-05-15 per `gh issue view 121`). Un-park trigger: a new `wr-risk-scorer` plugin release whose `agents/pipeline.md` (or `assess-release/SKILL.md` step 4) implements the Option 1 introducing-commit ancestry check (`git log --diff-filter=A --oneline -- <file>` plus `git merge-base --is-ancestor <commit> origin/<base>`); if ancestor, score release contribution at "already pushed; release queue pending drain only" residual ≤ 2/25; forward-looking risk treatment only when introducing commit NOT yet on origin. Local impact while parked: asymmetric false-high not false-low. AFK orchestrator's Step 6.5 above-appetite branch halts the loop or surfaces phantom remediations to user (per ADR-042); never costs a deploy. Composes with P028 (verifying 2026-05-16, TTL-band policy shipped upstream in v0.9.0) and P047 (parked 2026-05-30, assess-release SKILL.md step 5 prose) as third `wr-risk-scorer` plugin surface; P028 differs in that the upstream fix has shipped. README.md WSJF Rankings row removed (was tied at WSJF 6 cluster, four-way tie with P048 + P052 + P064 broken by ID-asc rule putting P046 at row 1); Parked-section row appended after P042 / before P047 per ID sort. Notes block P046 line retained (opened-context line still anchors the WSJF 6 backlog history). README-history.md rotated iter-14 P029 Open to Parked entry under 2026-05-31 heading per P134 last-reviewed-line discipline. AFK iter 15.

> Last reviewed: 2026-05-31 P048 (`/wr-itil:manage-problem` Step 5 P094 README refresh contract bypassed on bulk-creation paths) Open to Parked, upstream-blocked. Fix sites live in upstream `wr-itil` plugin across two surfaces: `~/.claude/plugins/cache/windyroad/wr-itil/<version>/skills/manage-problem/SKILL.md` Step 5 P094 block plus Step 4b multi-concern split contract (Option A end-of-flow batch refresh), AND `hooks/lib/create-gate.sh` per-session marker semantics (Option B per-ticket marker). Marketplace consumer cannot edit cached SKILL.md prose or hook scripts without losing the change on next plugin update. Verified 2026-05-31: latest cached `0.38.0` still ships the gap. `hooks/lib/create-gate.sh` lines 21 to 26 carry an explicit comment ("Per-session scope is intentional per architect direction A: a single `/wr-itil:manage-problem` invocation may write multiple tickets [Step 4b multi-concern split writes 2 to N consecutive `.open.md` files]; per-grep scope would block split-create after the first Write"). The architect has rejected Option B by design. `skills/manage-problem/SKILL.md` Step 5 P094 block (lines 513 to 529) implements Option A only for the SKILL-mediated `Step 4b` multi-concern split path ("the refresh fires **once** after all split tickets are written, not per-split, a single render captures the full new set in one pass"). Bulk-Write paths OUTSIDE the SKILL flow (retro sessions, post-mortem ticket capture, AFK orchestrator iterations creating multiple sibling tickets) still bypass P094 because the per-session marker covers all subsequent Writes without per-ticket P094 enforcement. Step 0 reconcile preflight (P118, lines 232 to 275) catches drift on the NEXT manage-problem call. Detect-on-next-invocation, not prevent-on-create. The preventive contract gap the ticket names remains. No upstream issue filed yet (same shape as P042 / P049 / P060 / P073 parks with upstream issue deferred). Un-park trigger: a new `wr-itil` plugin release whose `skills/manage-problem/SKILL.md` Step 5 P094 block extends end-of-flow refresh coverage to bulk-Write paths outside the SKILL flow (e.g. SKILL-level "end-of-flow trigger" the agent calls when wrapping a multi-ticket session), OR whose `hooks/lib/create-gate.sh` consumes the marker per ticket while preserving the Step 4b split-create path (e.g. counter-based marker that tracks a SKILL-known split-batch size). Local impact while parked: existing Workaround (run `/wr-itil:review-problems` periodically; rely on manage-problem Step 0 P118 preflight to catch drift on next invocation) remains the operating contract. Risk is detect-not-prevent; drift accumulates silently between bulk-Write sessions and the next manage-problem call, then Step 0 preflight catches it. Live evidence in the ticket body: commit 62f58aa (2026-04-27) created 11 tickets without README refresh; iter 2 preflight (commit 8352016, 2026-05-02) caught and repaired the 5-day drift. Composes with P031 (`reconcile-readme.sh` exit 127 on marketplace consumers, parked, upstream #85) and P049 (`reconcile-readme.sh` section-order false-positive STALE, parked) as third `wr-itil` plugin surface this AFK loop has touched; P031 plus P049 cover the reactive detect-and-repair surface, P048 covers the preventive P094 create-path contract: the two surfaces compose into the full create-and-detect drift discipline. Same marketplace-consumer-cannot-edit-cached-plugin shape as iters 3 to 15 parks. README.md WSJF Rankings row removed (was tied at WSJF 6 cluster, three-way tie with P052 + P064 broken by ID-asc rule putting P048 at row 1); Parked-section row appended after P047 / before P049 per ID sort. README-history.md rotated iter-15 P046 Open to Parked entry under 2026-05-31 heading per P134 last-reviewed-line discipline. AFK iter 16.



> Last reviewed: 2026-05-31 P052 (Ticket-family completeness check missing before declaring friction "done") Open to Parked, upstream-blocked. Fix sites live in upstream plugins across three SKILL.md files: `~/.claude/plugins/cache/windyroad/wr-itil/<version>/skills/transition-problem/SKILL.md` Pre-flight checks (Option b close-flow extension), `~/.claude/plugins/cache/windyroad/wr-itil/<version>/skills/review-problems/SKILL.md` Step 9d close-eligibility (Option b close-eligibility extension), and `~/.claude/plugins/cache/windyroad/wr-retrospective/<version>/skills/run-retro/SKILL.md` Step 4a (Option a retro-time advisory script). Marketplace consumer cannot edit cached SKILL.md prose without losing the change on next plugin update. Verified 2026-05-31: latest cached `wr-itil@0.38.0` plus `wr-retrospective@0.21.4` still ship the gap. `transition-problem/SKILL.md` Pre-flight checks (lines 75 to 94) are destination-specific (root-cause-identified for Open to Known Error; fix-implemented for Known Error to Verifying; user-confirmation for Verifying to Closed) with no sibling-family / friction-class scan. `review-problems/SKILL.md` Step 9d close-eligibility graph walk (line 58) explicitly states "Ignore `**Composes with**` (does not propagate)"; the proposed friction-class scan would need to extend the graph walk to honour Composes-with edges or add a separate keyword-scan layer. `run-retro/SKILL.md` Step 4a similarly has no friction-class advisory surface. No upstream issue filed yet (same shape as P042 / P049 / P060 / P073 / P048 parks with upstream issue deferred). Un-park trigger: a new `wr-itil` plugin release whose `transition-problem/SKILL.md` pre-flight or `review-problems/SKILL.md` Step 9d implements a friction-class sibling-family scan listing open / known-error siblings before allowing close, OR a new `wr-retrospective` release whose `run-retro/SKILL.md` Step 4a invokes an advisory script surfacing sibling-family completeness gaps inline. Local impact while parked: existing Workaround (manual at retro time: list all verifying tickets, scan keyword overlaps with `.open.md` and `.known-error.md` filenames before close; Tom did this in 2026-05-08 retro by deferring 10 close-candidates) remains the operating contract; risk is detect-by-discipline-not-by-skill. Live evidence in the ticket body: three friction classes (source-fetch, cover-image, editorial-meta) simultaneously misframed as "done" the same retro cycle. Composes with P031 (parked, upstream #85), P049 (parked), P048 (parked) as fourth `wr-itil` plugin surface this AFK loop has touched on the close-flow / review-problems contract: P031 reactive script-path; P049 section-order false-positive STALE; P048 P094 create-path; P052 sibling-family close-path. Same marketplace-consumer-cannot-edit-cached-plugin shape as iters 3 to 16 parks. README.md WSJF Rankings row removed (was tied at WSJF 6 cluster, two-way tie with P064 broken by ID-asc rule putting P052 at row 1; P064 now sole WSJF 6 row); Parked-section row appended after P049 / before P060 per ID sort. README-history.md rotated iter-16 P048 Open to Parked entry under 2026-05-31 heading per P134 last-reviewed-line discipline. AFK iter 17.

> Last reviewed: 2026-05-31 P064 + P071 WSJF re-rated (6.0 to 12.0 and 4.0 to 12.0) after user direction: newsletter critic should be simple S/W + optional context, not 38-check structured rubric (ADR-016 parameterised pattern is the root mechanism allowing the wrong shape). Both tickets now at top of WSJF Rankings as the strategic fix path. P064 description rewritten to "simplify newsletter critic; drop 38-check rubric"; P071 amended with re-rate evidence. ADR-016 amendment pending.

> Last reviewed: 2026-05-31 P074 deferred-re-rate executed: newly-captured external-comms marker-hook gap from today's P042 upstream-filing flow now scored Priority 6 (Medium), WSJF 3, M effort; Impact 3 (Moderate) x Likelihood 2 (Unlikely) reflects that the gate over-blocks rather than fails-open. P001 priority dropped to Very Low (WSJF 1) per the 2026-05-30 investigation note that the session-specific stuck-process hypothesis is unreproducible on Next 16; re-verify locally during normal working hours and transition to Verifying if it completes. No auto-transitions fired this pass: P064 + P071 are substance-confirm-pending on ADR-016 amendment per ADR-074; remaining Open tickets lack documented workarounds plus ready fix paths. 15 dev-work tickets ranked; 29 verifying; 15 parked.

## 2026-06-01

> Last reviewed: 2026-05-31 **README reconciled**: (1) drift entry corrected: P075 (newsletter drafter headings fail clarity test) added to WSJF Rankings as a deferred-WSJF placeholder (3, Severity 3, Effort M; re-rate at next /wr-itil:review-problems). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").

> Last reviewed: 2026-06-01 **README reconciled**: (3) drift entries corrected: P076 (newsletter pipeline drafts body before heading, HIGH priority per Tom direction; WSJF 12 matching P064 / P071 tier), P077 (voice and tone gate misses Tom-specific idioms not codified in the guide; WSJF 4.5), P078 (src/newsletters/published/leader/ folder is crowded; needs dated sub-directories; WSJF 8) added to WSJF Rankings. Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").

## 2026-06-02

> Last reviewed: 2026-06-02 (P078 worked under AFK iter 5): ADR-039 (per-date sub-directory layout for published newsletter editions) landed alongside the 6-edition `git mv` migration + SKILL.md / persona-config / READMEs / ADR-019 + ADR-026 + ADR-038 amendments in one related-cluster commit per ADR-014. P078 transitions Open -> Verifying. Drafts layout deferred (P078 Out-of-scope clause; separate decision). Triggers on next `/wr-newsletter` run resolving edition number against `<published-folder>/*/<YYYY-MM-DD>.md` glob AND next manual `mv` of a finalised edition into the new per-date sub-directory shape.

> Last reviewed: 2026-06-02 **README reconciled** (P081 work-problems iter 7): 1 drift entry corrected, namely P083 (ADR compendium docs/decisions/README.md is stale; lists 8 entries while ~40 ADR files exist on disk) added to WSJF Rankings as deferred-WSJF placeholder (1.5 = 3/2, Severity 3 Low, Effort M; re-rate at next /wr-itil:review-problems). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").

## 2026-06-03

> Last reviewed: 2026-06-03 **README reconciled** (P084 capture-problem follow-up to review-decisions drain): 1 drift entry corrected, namely P084 (Extend per-date sub-directory layout to src/newsletters/drafts/<persona>/) added to WSJF Rankings as deferred-WSJF placeholder (1.5 = 3/2, Severity 3 Medium, Effort M; re-rate at next /wr-itil:review-problems). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").

> Last reviewed: 2026-06-03 **README reconciled** (3-capture friction batch P085 + P086 + P087): 3 drift entries corrected, namely P085 (external-comms gate marker hash invalidation), P086 (P165 README-refresh-discipline hook over-fires), P087 (compendium emits em-dashes), all added to WSJF Rankings as deferred-WSJF placeholders (1.5 = 3/2 each, Severity 3 Medium, Effort M; re-rate at next /wr-itil:review-problems). Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").

## 2026-06-04

> Last reviewed: 2026-06-04 **README reconciled** (P076 SKILL.md implementation landed; Open to Verification Pending): 1 ticket transitioned, namely P076 (Newsletter pipeline drafts body before heading) removed from WSJF Rankings and added to Verification Queue at Released=2026-06-04. ADR-037 H1-first composition order implementation: `.claude/skills/wr-newsletter/SKILL.md` step 11 split into 11a (theme anchor + Tom-approval gate) + 11b (body draft) per Tom-pinned sub-decisions. Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").

> Last reviewed: 2026-06-04 **README reconciled** (P080 SKILL.md + agent implementation landed; Open to Verification Pending): 1 ticket transitioned, namely P080 (Newsletter pipeline has no cross-edition thesis-contradiction check) removed from WSJF Rankings and added to Verification Queue at Released=2026-06-04. ADR-038 cross-edition thesis-consistency gate implementation: new `.claude/agents/wr-newsletter-cross-edition-consistency.md` fresh-context subagent + new step 11.4 in `.claude/skills/wr-newsletter/SKILL.md` between 11b body draft and 11.5 URL verification. Tom-pinned sub-decisions: N=8 prior-edition window, block-save-on-CONTRADICTS, full prior-edition bodies. Reconciliation contract per P118 + ADR-014 amended ("Reconciliation as preflight robustness layer").

> Last reviewed: 2026-06-04 **review-problems pass**: backlog unchanged since today's earlier reconciliation; no Open to Known Error auto-transitions warranted. Step 4.6 relevance-close pass surfaced 5 multi-phase-mixed-progress caveat candidates: P064 (KE, 7/9 done, fix shipped 2026-06-02) routed Tom to KE-to-Verifying via separate `/wr-itil:transition-problem`; P055/P058/P065/P067 kept Open (ADR cites contextual not fix-shipped; P058 awaiting upstream agent-plugins#120, P065/P067 awaiting Tom direction). Step 2.5 transitive-effort pass produced zero re-rates (all intra-backlog Blocked-by upstreams parked/verifying/closed, contribute 0 per architect carve-out). Step 4 verification queue has zero `yes` candidates; no batch-close prompt fired. Step 4.5 inbound-discovery skipped per fail-soft (no `.upstream-channels.json`).

## 2026-06-14

> Last reviewed: 2026-06-04 **P064 verification pending**: Newsletter critic 38-check rubric simplification (fix shipped commit `5f2a527` on 2026-06-02; sw-critic rubric replaced with brief STRENGTHS + WEAKNESSES + optional RELEVANT CONTEXT prompt per ADR-035; wardley critic rubric similarly simplified; accepted_overrides parameter removed from SKILL.md steps 9 + 15; check_N references replaced with editorial-quality descriptions). Local-skill change, no changeset (sibling pattern: P062 + P063 + P066). Removed from WSJF Rankings; added to Verification Queue at Released=2026-06-02. Triggers verification on next `/wr-newsletter` run; verify via STRENGTHS + WEAKNESSES + CONTEXT shape outputs AND round-3 exhaustion frequency dropping relative to structured-rubric baseline (ADR-035 Confirmation criterion 5).

## 2026-06-15
> Last reviewed: 2026-06-15 **P089-P092 captured** (retro after The Shift Issue 09 publish): P089 wr-newsletter drafter structural + sourcing defects the five gates do not catch; P090 wr-newsletter CTA persistently includes the services pitch despite repeated correction; P091 wr-newsletter should ask the user for unresolvable source URLs instead of dropping attribution; P092 push:watch pull-rebase collides with amend-chains and false-fails on transient network errors. All deferred-rated, re-rate at next /wr-itil:review-problems.
> Last reviewed: 2026-06-15 (review-problems pass). Re-rated the 5 newest deferred captures: P089 to WSJF 8 (Sev 16, newsletter gate-weakness, recurring) now ranks #2; P090 + P091 to WSJF 6 (small-effort newsletter fixes); P092 to WSJF 4.5 (push:watch robustness); P088 to WSJF 3. Other 24 tickets carried forward at existing WSJF (not re-assessed this pass). Relevance-close pass surfaced 15 CLOSE-CANDIDATE-WITH-CAVEAT verdicts, ALL withheld as consumer-repo false-positives (file-no-longer-exists matched upstream packages/ paths absent here; ADR-shipped-confirmed matched context-mentions, not closure drivers); none auto-closed.

## 2026-06-16
> Last reviewed: 2026-06-15 **P091 fix committed -> Verification Pending**: added a human-in-the-loop terminal fallback to the wr-newsletter URL verification gate. `SKILL.md` step 11.5 (ADR-024) now routes an unresolvable URL (Google News bare domain, curl 403, Playwright fail, DuckDuckGo fail) through a batched `AskUserQuestion` for the canonical URLs before any drop; a citation is dropped only after the user declines. AFK carve-out (ADR-013 Rule 6) records unresolved citations in `.reviews.md` as `UNRESOLVED (awaiting user URL)` and surfaces at finalise rather than silent-drop. Step 11b drafter discipline gained a matching note; cross-references (does not duplicate) the P089 lint check (b). Architect + JTBD PASS; no new ADR; repo-local skill, no changeset. Awaiting next `/wr-newsletter` cycle to verify. (Prior: P090 fix committed -> Verification Pending; P089 fix committed -> Verification Pending; P071 closed decision-complete per ADR-079.)
> Last reviewed: 2026-06-16 (work-problems loop end). Captured P093 (newsletter lint check (b) should flag single bare unlinked outlets, per Tom's P089-review directive) at WSJF 3, and P094 (extend the domain-specific critic supersede to wr-blog, retire wr-sw-critic, per Tom's P071 follow-on confirmation) at WSJF 1.5. This loop closed P071 and shipped P089 + P090 + P091 to Verification Pending (newsletter structural-lint, CTA-pitch removal, ask-for-URLs fallback).
> Last reviewed: 2026-06-16 (work-problems loop-end retro). Captured P095 (dry-aged-deps deadlocks on exact-pinned deps that --update cannot bump, hit twice this session: playwright + react/react-dom) at WSJF 3. Appended cross-surface marker-hash evidence (gh-issue-create) to P085. Loop also closed P071, shipped P089 + P090 + P091 to Verification Pending, captured P093 + P094.
> Last reviewed: 2026-06-16 **P070 closed** - codified the three-deep-items-plus-notes editorial discipline in `.claude/skills/wr-newsletter/assets/draft-template.md`: five edits collapse the three contradictory count lines into a single ADR-032-referenced depth rule (roughly three deep items each a variation on one thesis; every other filtered story demoted to "Also worth noting", not dropped; isolated Disclosure line; soft cap at four, external review at five-plus). Reframed as a DEPTH discipline not a content cap, so it holds Tom's pinned no-content-cap direction. All four 2026-05-30 architect/JTBD blockers dissolved by confirmed ADR-032. Architect ALIGNED, JTBD + voice-tone PASS, style-guide N/A. Open to Closed bypassing Verifying (doc-class edit, live on commit). Prior: **P079 to Verification Pending** - reduced the `.linkedin.md` sibling template to frontmatter + post body only (removed the `## LinkedIn Post` wrapper, `## Image` block, `## Notes for posting` block, and the manual link line) across `.claude/skills/wr-newsletter/SKILL.md` steps 15.5/16/17. Hardcoded auto-share shape chosen over a persona-config field; canonical target is the hand-edited Issue 08 sibling. Architect ALIGNED + JTBD + voice-tone PASS, style-guide N/A. Open to Verification Pending (fix live in repo skill on commit; awaiting next finalise run for zero-deletion verification). Impact 2-to-3/4 elevation left for review-problems. Prior: **P092 to Known Error** - root cause confirmed and fix implemented in `scripts/push-watch.sh` (B sibling-amend rebase guard; C transient-error retry around `gh run watch`, now including the HTTP 401 "Bad credentials" blip per fresh evidence on run 27609565746). Known Error multiplier applied: WSJF 4.5 to 9.0, top of the dev-work queue. Committed, awaiting orchestrator-owned push to release (then moves to Verification Pending per ADR-022). Prior: **P065 closed (resolved-by-decision)** - Tom decided Option D (2026-06-15): keep Grade 10 for the leader-register cog-a11y gate, accept the recurring NEEDS_REVISION_OPTIONAL advisory verdict, no code/persona/ADR change. Open to Closed bypassing Verifying (chosen option is the status quo, no fix to verify). Architect + JTBD gates PASS; human direction supersedes the prior Option-A advisory lean per ADR-066 / ADR-074.

## 2026-06-17

> Last reviewed: 2026-06-16 **P093 closed** - extended newsletter structural-lint check (b) in `scripts/check-newsletter-structure.sh` to flag a single bare unlinked outlet (not only 2+), per Tom's P089-review directive (do not name a news site without linking it). Restructured to a per-item linked-outlet accumulator with a same-item back-reference carve-out (by outlet name, canonical domain, and the finance.yahoo.com->Bloomberg / yahoo.com/news->Axios syndication map). 3 new behavioural vitest cases + unchanged Issue-09 fixture, 16/16 green. Architect + JTBD PASS, no new ADR (routine refinement of the ungoverned P089 gate). Open to Closed bypassing Verifying (deterministic repo-local script, live on commit, test-verified).

> Last reviewed: 2026-06-17 **P096 captured** - work-problems orchestrator re-selects direction-blocked tickets as highest-WSJF every loop, producing no-op skips until the user answers (lightweight aside via /wr-itil:capture-problem).

> Last reviewed: 2026-06-17 **P097 captured** - upstream wr-itil check-upstream-responses.sh writes a U+2014 em-dash into the audit-log heading, tripping adopter no-em-dash hooks every Step-0d pass (lightweight aside via /wr-itil:capture-problem).

> Last reviewed: 2026-06-17 **P092 to Verification Pending** - push:watch robustness fix released. `scripts/push-watch.sh` adds `is_sibling_amend()` (skips the auto pull-rebase on an amend-of-a-just-pushed-commit, no more conflict-marker injection, B) and `is_transient_gh_error()` + `watch_run_resilient()` (re-check real run state with bounded backoff so a transient gh-poll blip no longer false-fails the pipeline, C). Shipped in commit `adbdb72` (2026-06-16), live on origin/master; 8 vitest cases green, CI green. Repo-local script, no changeset (release == pushed-to-origin per ADR-022). Known Error to Verification Pending; awaiting Tom's production verification.

## 2026-06-22

> Last reviewed: 2026-06-17 **P098 captured** - work-problems Step 6.5 post-release K->V auto-transition has no vehicle for repo-local-script fixes in a consumer repo with no npm release (lightweight aside via /wr-itil:capture-problem).
> Last reviewed: 2026-06-22 **P099-P102 captured** - Issue 10 retro: full-gate-rerun discipline after post-finalise edits, cover/lint silent format traps, human-code-review + generic-CTA editorial drift, gates don't fact-check unsourced analogies (via /wr-retrospective:run-retro).

## 2026-06-24

> Last reviewed: 2026-06-22 **batch transition** - P089 close, P091 close, P092 close, P034 close (verified in-session during Issue 10 run + retro per run-retro Step 4a).

## 2026-06-27

> Last reviewed: 2026-06-27 **P099 transitioned Open to Verifying** (post-gate edit discipline shipped: wr-newsletter SKILL.md §15.6 + failure-modes ref; any post-gate brief/LinkedIn edit re-enters the FULL gate set, not voice+lint; I13 RFC-trace gate non-actionable per P104/P070 precedent).

> Last reviewed: 2026-06-24 **P103 captured** - Assistant asserts negative project-state claims from an incomplete search without verifying or asking (lightweight aside via /wr-itil:capture-problem).

> Last reviewed: 2026-06-27 **P006 closed** (OG-image drift guard verified in-session: shared siteCopy.mjs constants + reproduction test green) + **inbound-discovery bootstrapped** (windyroad/windyroad issues channel, first poll 0 reports) + relevance-close pass 0 closes (all evaluator candidates false-positive on verify).

> Last reviewed: 2026-06-27 **P103 transitioned Open to Verifying** (negative/absence-claim verify-before-asserting discipline shipped: CLAUDE.md + memory `feedback_verify_project_state_before_writing.md` extended; I13 RFC-trace gate non-actionable per P070 precedent, RFC tier unadopted in this repo).

> Last reviewed: 2026-06-27 **P104 captured** - I13 RFC-trace predicate and manage-problem I13 gate are not adopter-aware (fire no-rfc-trace in repos without an RFC tier) (lightweight aside via /wr-itil:capture-problem).

> Last reviewed: 2026-06-27 **P105 captured**: risk-score-commit-gate RISK-POLICY staleness threshold too tight (14 days) + no AFK-satisfiable refresh path; quarterly cadence re-attested into RISK-POLICY.md, gate-threshold change tracked as upstream wr-risk-scorer work (lightweight aside via /wr-itil:capture-problem).
