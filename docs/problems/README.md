# Problem Backlog

> Last reviewed: 2026-05-12 P010 (source-tier fetch blocked by OpenAI bot protection + Reddit tool-layer refusal) re-rated 9.0 to 4.5 via P076 transitive-effort rule. Added formal `## Dependencies` block to P010 (Blocked by P014) and mirror `Blocks: P010` to P014. Effort propagates marginal M to transitive L through the P014 dependency since the Reddit half of P010 has no in-pipeline workaround absent P014's Playwright helper. Honest WSJF prevents P010 out-ranking its blocker. Ticket order at the 4.5 tier: P010 (Known Error, L) before P050 (Open, M) per the canonical tie-break ladder.
> Run `/wr-itil:review-problems` to refresh WSJF rankings.

## WSJF Rankings

Dev-work queue only. Verification Pending (`.verifying.md`, WSJF multiplier 0) and Parked (`.parked.md`, multiplier 0) tickets are excluded per ADR-022 and surfaced in their own sections below. Sorted WSJF descending.

| WSJF | ID | Title | Severity | Status | Effort | Reported |
|------|-----|-------|----------|--------|--------|----------|
| 8.0  | P021 | architect-mark-reviewed strict-verdict-string parsing under-counts affirmative ISSUES FOUND verdicts as FAIL | 8 (Minor) | Open | S | 2026-04-26 |
| 8.0  | P028 | risk-scorer 30-min TTL expired during long-running orchestrator turns | 8 (Minor) | Open | S | 2026-04-26 |
| 8.0  | P033 | report-upstream SKILL.md Step 5 example uses --label flag that fails when upstream repo hasn\'t pre-created the label | 8 (Minor) | Open | S | 2026-04-27 |
| 8.0  | P042 | jtbd-enforce-edit hook uses relative docs/jtbd path; fails when cwd is not project root | 8 (Minor) | Open | S | 2026-05-01 |
| 6.0  | P014 | Playwright helper for tool-blocked sources | 12 (Significant) | Known Error | L | 2026-04-19 |
| 6.0  | P022 | architect-refresh-hash.sh only refreshes hash on docs/decisions/* writes | 6 (Minor) | Open | S | 2026-04-26 |
| 6.0  | P027 | work-problems Step 5 exit-code rule does not handle is_error:true transient API failures | 6 (Minor) | Open | S | 2026-04-26 |
| 6.0  | P047 | wr-risk-scorer:assess-release SKILL.md step 5 contract violation (Skill-tool prose vs Agent-tool parameter) | 6 (Minor) | Open | S | 2026-05-02 |
| 6.0  | P049 | reconcile-readme.sh section-order assumption produces false-positive STALE for tickets in section-after-Closed | 6 (Minor) | Open | S | 2026-05-02 |
| 6.0  | P046 | Risk scorer treats changesets as queued when underlying commits are already on origin | 12 (Significant) | Open | M | 2026-05-02 |
| 6.0  | P051 | Newsletter tier-1 source list misses US regulatory-news outlets | 12 (Moderate) | Open | M | 2026-05-08 |
| 6.0  | P052 | Ticket-family completeness check missing before declaring friction done | 12 (Moderate) | Open | M | 2026-05-10 |
| 6.0  | P053 | /wr-newsletter formal gate sequence missing a cog-a11y step | 12 (Moderate) | Open | M | 2026-05-10 |
| 4.5  | P010 | Source fetch blocks OpenAI and Reddit | 9 (Moderate) | Known Error | L (transitive via P014) | 2026-04-17 |
| 4.5  | P050 | Assistant scope-creeps on user-flagged corrections, changing more than asked | 9 (Moderate) | Open | M | 2026-05-08 |
| 4.0  | P029 | work-problems iteration boundary leaves run-retro BRIEFING.md edits uncommitted | 8 (Minor) | Open | M | 2026-04-26 |
| 3.0  | P001 | Next.js build hangs locally | 6 (Minor) | Open | M | 2026-04-14 |
| 3.0  | P023 | architect-gate drift detection rm\'s marker without offering recovery path | 6 (Minor) | Open | M | 2026-04-26 |
| 3.0  | P045 | Assistant accepts ticket Fix Strategy upstream-placement framing without questioning | 6 (Minor) | Open | M | 2026-05-02 |
| 3.0  | P048 | /wr-itil:manage-problem Step 5 P094 README refresh contract bypassed on bulk-creation paths | 6 (Minor) | Open | M | 2026-05-02 |
| 2.0  | P030 | work-problems SKILL.md marker wording uses em-dash, forces whitelist in PostToolUse:Bash em-dash hook | 4 (Low) | Open | M | 2026-04-26 |
| 1.5  | P054 | work-problems Step 1 ranking does not exclude just-worked Known Error tickets awaiting orchestrator-owned push and transition | 3 (Medium) | Open | M | 2026-05-12 |
| 1.5  | P055 | AFK iter subprocess `Stream idle timeout` API errors recur at high rate | 3 (Medium) | Open | M | 2026-05-12 |

## Verification Queue

Fix released, awaiting user verification. Ranked by release age, oldest first per ADR-022. `Likely verified?` flags tickets >= 14 days since release (P048 Candidate 4). **Note**: per the 2026-05-08 retro marquee finding (P052), close-on-age alone is insufficient; sibling-family completeness should be checked before close. P016 and P036 are flagged with regression evidence from the 2026-05-08 retro and should NOT close until the regression is addressed.

| ID | Title | Released | Fix summary | Likely verified? |
|----|-------|----------|-------------|------------------|
| P006 | OG share image does not track homepage copy pivots | 2026-04-15 | Short-term fix in commit `754a04a` updated the hardcoded SVG in `scripts/generate-og-image.mjs` to the patch-fitness headline ("You're taking too long to patch your software.") and removed the personal attribution footer (team voice per ADR 010); PNG regenerated. Permanent fix in commit `100df63` extracted the hero headline + subtitle into shared `src/lib/siteCopy.mjs` constants consumed by both `src/app/page.tsx` and `scripts/generate-og-image.mjs`; reproduction test `src/lib/siteCopy.test.ts` asserts the shared-import contract. Both on `origin/master`. Verification triggers: (1) production deploy of origin/master to https://windyroad.com.au, (2) flush LinkedIn's OG cache via https://www.linkedin.com/post-inspector/ (Inspect then Re-scrape), (3) visual confirmation that the OG preview renders the patch-fitness headline with no personal attribution footer. AFK iter 2 cannot exercise (no browser, no production deploy trigger). | yes (26 days) |
| P012 | No CI-status check on push/release; conditional-commitment verification missing | 2026-05-11 | Shipped windyroad-local defence-in-depth `scripts/ci-status-check.sh` per new ADR-028 (proposed); wired into `scripts/push-watch.sh` (current branch) and `scripts/release-watch.sh` (master). Bypass marker `red-ci-acknowledged` lives in wrapper-owned `/tmp/wr-push-watch-${SESSION_ID}/` namespace (no layering violation against upstream plugin's `/tmp/risk-${SESSION_ID}/`). Non-fatal degradation on `gh` failure mirrors ADR-021's robustness shape. Architect PASS (with revisions applied: shared script, wrapper-owned marker dir, new ADR not extension). JTBD ALIGNED (operator scope; protects windyroad.com.au integrity). Conditional-commitment verification piece remains split-out per 2026-04-27 rescope. Verification triggers on next push:watch or release:watch attempt against red CI on the relevant branch, OR upstream `windyroad/agent-plugins#86` landing (which would retire the wrapper check per ADR-028 reassessment criteria). | no (0 days) |
| P036 | /wr-newsletter drafter leaks editorial-process meta-commentary into reader-facing body | 2026-05-07 | Added "No editorial-process meta-commentary" rule to step 11 voice rules with five banned-pattern categories (corroboration assurance, editorial-cycle time-window tags, evidence-stance hedge prose ABOUT the rating system, business-state disclosure, surfacing-attribution prose). JTBD carve-outs preserve evidence-stance labels (JTBD-205) and Source-line attribution (JTBD-203 + check_4). Interim discipline pattern (mirrors ADR-019); reassessment trigger after 4 editions if corrections-per-edition does not drop below 1. | no (3 days) |
| P038 | /wr-newsletter step 16 mandates inline review blocks in brief; creates confirmation-bias risk | 2026-05-07 | Step 16 rewritten per ADR-026: brief now frontmatter + body + CTA only; six review classes plus LinkedIn-post voice review now live in `<publication-date>.reviews.md` sibling. Bundled with P040 + P041 per ADR-014. Verification triggers on tonight's prep + tomorrow's finalise. | no (3 days) |
| P040 | /wr-newsletter draft files use prep date instead of publish-Friday date | 2026-05-07 | Added `<publication-date>` step-0 binding (next-Friday-from-local-time, with Sat/Sun forward-resolution); replaced `YYYY-MM-DD` placeholders across steps 0, 10, 11, 12, 16, 17. Verification triggers on tonight's prep (2026-05-07 Thu) writing files named `2026-05-08.X` not `2026-05-07.X`. | no (3 days) |
| P041 | /wr-newsletter step 16 keeps LinkedIn post + image notes inline in brief; risks accidental publication | 2026-05-07 | LinkedIn post body + image + alt text + posting notes now live in `<publication-date>.linkedin.md` sibling per ADR-026; LinkedIn-post voice review specifically placed in `.reviews.md` (not `.linkedin.md`) per architect direction for fresh-context discipline. Bundled with P038 + P040. Verification triggers on tomorrow's finalise. | no (3 days) |
| P002 | Hero content extends beyond the fold | 2026-04-25 | Hero fold-fit SCSS fix shipped in commit `72c3c2b` on `origin/master`: `min-height: 45vh` to `100svh` (mobile breakpoint 35vh to 100svh too), `padding-bottom: 4rem` added to balance existing top-padding for true vertical centring, `@media (max-height: 600px) { min-height: auto; }` short-viewport guard for WCAG 1.4.10 Reflow at 400% zoom per accessibility-lead conditional PASS. Verification triggers on next interactive session: 1080p desktop (1920x1080) all hero content above fold, mobile (Safari iOS, Chrome Android) hero fills viewport with browser chrome, 200% / 400% zoom on 1280x720 scrollCue does not overlap CTAs / Countdown, vertical centring visually balanced. AFK iter 2 cannot exercise (no browser). | yes (16 days) |
| P016 | Filter drops significant stories without corroboration | 2026-04-25 | Added step 4b corroboration sub-step (Google News RSS) and step 10 weak-attribution escalation to wr-newsletter SKILL.md | yes (15 days) |
| P018 | Newsletter publication time pressure on Friday | 2026-04-25 | Added phase=prep, phase=finalise, phase=full to wr-newsletter SKILL.md with .prep.md state handoff per ADR 017 | yes (15 days) |
| P011 | Visual artifacts shipped without render-verify | 2026-04-25 | Added render-and-verify discipline note to wr-newsletter SKILL.md step 12 and created shared `scripts/render-svg.mjs` helper that wraps `sips` for SVG-to-PNG conversion | yes (15 days) |
| P009 | Content-risk inline self-scoring | 2026-04-25 | Subagent-ised content-risk gate per ADR 018: new rubric at `.claude/skills/wr-newsletter/assets/content-risk-rubric.md`, new agent at `.claude/agents/wr-content-risk-scorer.md`, SKILL.md step 14 + 14-prime invoke the agent | yes (15 days) |
| P013 | No voice/tone gate on external comms | 2026-04-25 | Added voice gate to wr-newsletter SKILL.md step 15.5 (Draft the LinkedIn post) mirroring step 13\'s pattern; step 16 save-block layout updated to capture the LinkedIn-post voice block. Investigation Tasks 2 and 4 (PreToolUse hook for `gh`/`npm` and validation against past comms) deferred to P012 ship-gate. | yes (15 days) |
| P015 | Drafter paraphrases interactive capture text | 2026-04-25 | Parts 1 + 2 released to wr-newsletter SKILL.md per new ADR 019: step 10 writes `<draft-folder>/YYYY-MM-DD.capture.md` after per-item capture; step 10-prime appends during finalise with missing-file AskUserQuestion branch; step 11 adds Capture fidelity rule (preserve load-bearing noun-phrases verbatim from Adjust text); step 17 mentions transcript path and bundled-move reminder. Part 3 (`check_32` rubric check) deferred until baseline of editions-with-transcript exists; ADR 016 amendment paired with check_32 when taken up. | yes (15 days) |
| P017 | Critic rubric misses contradictions and ambiguity | 2026-04-25 | Third-wave rubric expansion: 7 new checks (32-38) added to `.claude/skills/wr-newsletter/assets/newsletter-critic-rubric.md` (31 to 38 checks); 2 mirror checks (17-18 internal consistency + referential clarity) added to `.claude/skills/wr-newsletter/assets/wardley-critic-rubric.md` (16 to 18 checks). Round-specific exit criteria updated in both files. | yes (15 days) |
| P008 | Critic rubric misses external review findings | 2026-04-25 | Editorial-sim subagent released per new ADR 020: new agent at `.claude/agents/wr-newsletter-editor.md` reads persona JTBD context and emits `EDITOR_REVIEW` block with would-open / would-read-through / would-forward axes; SKILL.md step 15.25 + 15.25-prime invoke the agent between sw-critic and LinkedIn-post drafting; runs only when sw-critic returns PASS. Verification triggers on next `/wr-newsletter` run. | yes (15 days) |
| P019 | Claude does not proactively act on ambient dirty state during retrospective and wrap-up | 2026-04-26 | Added feedback memory note `feedback_act_on_ambient_dirty_state_at_wrap_up.md` to user memory tree directing the assistant to commit or revert ambient dirty state directly at wrap-up, retro, or AFK iteration boundary when intent is obvious; AskUserQuestion reserved for genuine ambiguity. Verification triggers on next AFK retro or wrap-up turn that finds dirty state. | yes (14 days) |
| P020 | No dependency update cadence; AFK loop halts on stale-dep gate | 2026-04-26 | Added `.github/workflows/deps-refresh.yml` weekly cron (Mondays 09:00 UTC) running `npx dry-aged-deps --update --yes` and opening a PR via `peter-evans/create-pull-request@v7` when root manifests change. New ADR 022 records the proactive cadence sibling to ADR 021\'s reactive bypass. Verification triggers on first scheduled run or manual `gh workflow run deps-refresh.yml` dispatch, plus a future AFK drain that does not halt on accumulated drift. | yes (14 days) |
| P024 | .claude/** Edit/Write permission gate not satisfied by */Edit allow list | 2026-04-27 (attempt 2 colon-form) | Released 2026-04-27 by appending colon-form matchers `Edit:.claude/skills/**` and `Write:.claude/skills/**` to `.claude/settings.json` alongside the existing parenthesised `Edit(.claude/**)` / `Write(.claude/**)` entries. Empirical verification PASS for `.claude/skills/**` subtree across 2026-05-11 AFK iters 3 to 6 (commits `fcd87e4`, `4dd2485`, `586c21e`, `95cd9c6` all edited `.claude/skills/wr-newsletter/*` paths without python3 heredoc fallback; no P024 friction cited in any iter retro). `.claude/agents/**` subtree verification queued behind a future intentional agent-file edit (architect-mandated A/B control surface to distinguish "colon-form is the differentiator" from "global `.claude/**` block"). Closure trigger: one additional AFK iter that edits a `.claude/agents/*.md` path cleanly without python3 fallback. | no (skills verified, agents pending A/B carve-out) |
| P025 | no-em-dash.sh has zero coverage of Bash-path file writes | 2026-04-26 | PostToolUse:Bash hook `.claude/hooks/no-em-dash-bash.sh` added and wired into `.claude/settings.json` PostToolUse section, matcher `Bash`. Hook drains the PostToolUse JSON payload, scans the working tree via `git status --porcelain` plus `git diff` for added-line filtering, and blocks on U+2014 in modified files. The hook contains zero literal em-dashes (search bytes built via `printf '\\xe2\\x80\\x94'`). A narrow whitelist exempts the contractual `- **Upstream report pending** (em-dash) external dependency identified` marker that the upstream `wr-itil:work-problems` SKILL appends on `upstream-blocked` parks; whitelist removal tracked in P030. Architect PASS (precedent-following Pre+Post pairing per ADR-006), JTBD PASS (internal toolchain). Five inline test cases verified during implementation: clean tree, untracked-with-em-dash, untracked-marker-only, untracked-marker-plus-extra, tracked-diff-em-dash. Verification triggers on the next AFK iteration that issues a Bash-path file write with an em-dash and gets blocked, plus a deliberate orchestrator marker-append flow that gets through unblocked. | yes (14 days) |
| P035 | /wr-newsletter drafter paraphrases quantitative claims away from source | 2026-05-02 | Added "Source-article quantitative-claim fidelity (P035, interim)" sub-heading to `.claude/skills/wr-newsletter/SKILL.md` step 11, after the existing ADR-019 capture-fidelity block. Directs the drafter to preserve counts, percentages, ratios, ranges, and currency values verbatim from the source article body (no rounding, no range-collapse, no duplicating one value across distinct denominators), and to fall back to qualitative phrasing when the article body is unavailable. Framed as interim defence-in-depth that retires when ADR-024 confirmation criterion 1 is met (URL verification gate exercised across one full prep-finalise cycle). Architect ALIGNED on revised plan (initial review found four issues, all addressed: separate sub-heading rather than ADR-019 expansion, ADR-024 cross-reference, sunset condition documented). JTBD PASS across JTBD-200 / JTBD-203 / JTBD-205 / JTBD-001 / JTBD-003. Verification triggers on the next `/wr-newsletter` run that drafts a quantitative claim from a source article body. | no (8 days) |
| P034 | /wr-newsletter generates plausible-but-invented URLs and skips verification before save | 2026-05-12 | Released to `origin/master` in commit `4dd2485`: step 11.5 URL verification gate with scope plus transport selection (Playwright for JS-protected publishers, curl for static / RSS-style, DuckDuckGo HTML for Cloudflare-blocked) plus fresh-context semantic subagent contract (SUPPORTED / REFUTED / NOT MENTIONED) plus save-gate semantics plus phase variant 11.5-prime plus audit-trail table in `.reviews.md` per ADR-024 + ADR-026. Verification trigger: next prep + finalise cycle without user URL intervention plus edition zero-invented-URL spot-check. | no (0 days) |
| P037 | /wr-newsletter cover-image step requires 15+ iteration rounds; brand-asset grep + font-rendering diagnostics missing | 2026-05-12 | Released to `origin/master` in commit `586c21e`: step 12 of `.claude/skills/wr-newsletter/SKILL.md` rewritten into five labelled sub-steps (12.a mandatory brand-asset read with named canonical paths; 12.b typography conventions; 12.c sips silent-substitution traps; 12.d compose-the-cover; 12.e P011 render-and-verify). Verification trigger: next finalise producing the cover in fewer than 5 iteration rounds with no brand-mark, monogram, or font-weight reverts. Supersedes-by-P044 noted in ticket body (P044's templated skill is the structural successor; P037 is the interim documentation hardening). | no (0 days) |
| P039 | sw-critic round-3 REJECTED-with-author-overrides verdict obscures publish-decision signal | 2026-05-12 | Released to `origin/master` in commit `fcd87e4`: PASS_WITH_AUTHOR_OVERRIDES verdict variant added to `.claude/agents/wr-sw-critic.md` per ADR-025; caller plumbing landed in `.claude/skills/wr-newsletter/SKILL.md` (steps 9, 15, 15.25, 17, Failure modes). Verification trigger: next weekly newsletter edition surfacing a publish-decision verdict with documented overrides rather than a misleading REJECTED. Follow-up runtime auto-degrade threshold (user-direction 2026-05-11) tracked separately. | no (0 days) |
| P044 | /wr-newsletter needs a dedicated cover-image skill (templated SVG + render script) for consistent weekly output | 2026-05-12 | Released to `origin/master` in commit `95cd9c6`: wr-newsletter-cover skill with templated SVG (`assets/cover-template.svg` with placeholders for title, desc, wordmark, subtitle, two-line hook, week-ending) plus render script (`scripts/render-cover.mjs` shells out to `render-svg.mjs` for sips PNG) plus vitest unit test (4 passing). `/wr-newsletter` step 12 collapsed from 5-stage inline prose to single skill invocation; step 12-prime delegates re-render on finalise headline change. Verification trigger: next prep+finalise cycle producing the cover in 1 invocation with no brand-mark, monogram, or font-weight reverts. | no (0 days) |
| P043 | /wr-newsletter three-lens scoring lets non-leader-actionable items through; persona relevance is post-filter, not in scoring | 2026-05-12 | Released to `origin/master` in commit `219377b`: each T / O / H lens now requires a `named_action` for the `<target-reader>` this quarter from a persona-conditional action vocabulary (leader: procurement, capability investment, risk audit, etc.; developer: spot-test, adopt, pilot, watch, skip, abstain, upward-influence per JTBD-203, etc.). `.claude/skills/wr-newsletter/assets/three-lens-filter.md` rewritten with vocabulary table; `SKILL.md` step 4 bullet 2 amended with in-flow enforcement pointer. Third-tier prioritisation rewritten to use lens-level gating (P016 no-map-anchor safety net preserved). Verification trigger: next weekly newsletter edition whose candidate metadata records a named_action per yes-scored lens and whose third-tier prioritisation uses the lens-level gate. | no (0 days) |

## Closed

Closed tickets are listed for audit but excluded from the active backlog:

- P003 (Countdown slider left-aligned)
- P004 (Slider position misleading for probability)
- P005 (Slider has no visual stop indicators)
- P007 (wr-sw-critic agent not discoverable in session)
- P032 (Assistant writes ticket bodies and claims about project state without verifying current code/config first). Closed 2026-05-02 on session-evidence per run-retro Step 4a. Verify-before-assert discipline exercised five times in iter 1: read SKILL.md, plugin-cache versions, gh issue 85 state, packages/ absence, gh repo ownership before any ticket-body or external claim landed.
- P026 (dry-aged-deps pre-push gate has no AFK-bypass path). Closed 2026-05-07 as misframed on Tom's direction. Gate is intentional discipline (per the brushing-teeth analogy); the "AFK bypass" is doing the upgrades the gate exists to enforce. P020 (verifying) covers the legitimate proactive-cadence complement; deps-refresh.yml has kept the gate green since shipping. Principle captured in user memory `feedback_dry_aged_deps_is_intentional_discipline.md`.

## Parked

Parked tickets are excluded from WSJF ranking. They surface here so the upstream blocker stays visible.

| ID | Title | Reason | Parked since |
|----|-------|--------|--------------|
| P031 | manage-problem Step 0 reconcile-readme.sh hits exit 127 on marketplace consumers | upstream-blocked (windyroad/agent-plugins#85) | 2026-05-02 |

## Notes

- **P012 (rescoped 2026-04-27)**: original framing claimed push/publish/deploy were not gated. Direct read of `~/.claude/plugins/cache/windyroad/wr-risk-scorer/0.4.1/hooks/git-push-gate.sh` confirmed push and release ARE gated on the risk-scorer (table inside ticket). publish + deploy are CI-only for this project. Real gaps narrowed to (a) no CI-status check on push/release (extend gate to call `gh run list --branch master --limit 1 --json conclusion,status` and block on `conclusion: failure`), and (b) conditional-commitment verification (split out as a separate ticket; XL open research, different problem class). Effort L to S after rescope; WSJF 6.0 to 24.0. Cross-reference with the 2026-04-27 retro session.
- **P031 (parked 2026-05-02)**: marketplace consumers of `wr-itil:manage-problem` hit exit 127 on Step 0 because the SKILL.md hardcodes the vendored `packages/itil/scripts/reconcile-readme.sh` path. Fix is to resolve via `${CLAUDE_PLUGIN_ROOT}/scripts/reconcile-readme.sh` (the plugin loader sets the env var consistently for both vendored and marketplace surfaces). Reported upstream as windyroad/agent-plugins#85 on 2026-04-27; verified persistence in cached plugin version 0.23.1 on 2026-05-02 (issue still OPEN, no comments). Parked because the genuine fix is upstream-only and a project-local wrapper would manufacture a fake `packages/itil/` source tree in this marketplace consumer (contradicts ADR-009). Un-park trigger: a plugin release lands with the env-var resolution. Composes with P027 (same plugin upstream).
- **P032 (opened 2026-04-27)**: assistant pattern of asserting hook/gate/config state from memory of related tickets rather than reading actual files; user has had to correct three times in this single session (P012 framing, .claude/** scope, architect-PASS marker semantics). Three-lever fix: (1) memory note `feedback_verify_project_state_before_writing.md`, (2) CLAUDE.md discipline section, (3) optional UserPromptSubmit hook. Composes with P019 (same family of assistant-discipline gaps).
- **P045 (opened 2026-05-02)**: sibling discipline ticket to P032 on a different surface. Pattern: assistant accepts a ticket's `Fix Strategy` upstream-placement claim ("fix is in upstream X plugin") as authoritative spec rather than as a hypothesis to verify, propagating it into wrong WSJF re-rates and wrong upstream-blocked classifications. Live example surfaced this session in AFK iter 2 working P026: iter accepted the "wr-itil:update-stale-deps in agent-plugins" framing as fact, re-rated S to XL on transitive-via-upstream basis, classified upstream-blocked. Tom corrected mid-loop: (a) we cannot dictate placement to agent-plugins maintainers, (b) the skill probably does not belong in agent-plugins at all because it operates on this project's package.json, lockfile, and test suite. Three-lever fix mirrors P032: (1) discipline note in memory + CLAUDE.md "Verify before asserting" extension covering placement-verification, (2) check at manage-problem review path, (3) check at work-problems Step 5 iter prompt. Default: ship lever 1, ship 2/3 only if pattern recurs. P026 updated this session to reflect windyroad-local placement; Effort and WSJF flagged TBD pending re-rate.
- **P046 (opened 2026-05-02)**: risk scorer pipeline subagent reads `.changeset/*.md` files as forward-looking "would deploy" signals without verifying whether the introducing commits are already on `origin/<base>`. Live example this session: scorer returned `release=8` above appetite for three changesets whose underlying commits (`d6cc6d9`, `b196c18`, `b97bc0b`) were already pushed to master and live on the site; the changesets-action release PR was sitting on `origin/changeset-release/publish` awaiting drain only. Tom corrected: "pause-commercial-funnel-fully-booked has already been pushed." False-high score routes downstream consumers (work-problems Step 6.5 above-appetite branch, ADR-042 auto-apply) into the wrong code paths. Two fix options: (1) scorer-side commit-state check via `git merge-base --is-ancestor <introducing-commit> origin/<base>` rule, (2) caller-side defensive verification. Default: option 1, single-rule extension to scorer prompt. Per P045 discipline, placement (upstream wr-risk-scorer subagent prompt vs windyroad-local rubric amendment) is the maintainer's call; default upstream until evidence shows otherwise. Composes with P028 (same plugin, different surface), P045 (the discipline applied to classify this fix without pre-emptive upstream-blocked).
- **P019-P029**: opened 2026-04-26 in a pre-loop setup pass capturing 11 signals from the prior AFK 11-iteration retrospective. Three architect-gate-family tickets (P021 strict-verdict parser, P022 narrow refresh scope, P023 missing drift-recovery path) compose with each other; closing P021 unblocks P022\'s effective trigger surface and P023\'s recovery shape. Two dependency-cadence tickets (P020 no cadence, P026 no AFK-bypass) compose; closing P020 reduces P026 trigger frequency. P024 (.claude/** permission denials) blocks P025 (no-em-dash.sh Bash-path coverage gap) because the python3 workaround needed for P024 is what bypasses the em-dash hook. P028 (risk-scorer TTL) and P029 (BRIEFING hand-off commits) compose; closing P029 reduces P028 trigger frequency.
- **P020**: fix released 2026-04-26 (verifying). Proactive cadence shipped as `.github/workflows/deps-refresh.yml` weekly cron per new ADR 022, sibling to ADR 021\'s reactive bypass in `scripts/push-watch.sh`. Together they address both halves of the dry-aged-deps gate surface: cadence prevents accumulation, push-watch absorbs whatever drift accumulated since the last cadence run. Verification fires on the first scheduled Monday run or manual `gh workflow run deps-refresh.yml` dispatch.
- **P006**: short-term content fix landed 2026-04-15 in commit `754a04a` (hardcoded SVG updated to patch-fitness headline; team-voice attribution removed per ADR 010); permanent fix landed 2026-04-15 in commit `100df63` (shared `src/lib/siteCopy.mjs` constants consumed by both `src/app/page.tsx` and `scripts/generate-og-image.mjs`, plus `src/lib/siteCopy.test.ts` reproduction test asserting the shared-import contract). Transitioned Known Error to Verification Pending on 2026-05-11 (AFK iter 2) per ADR-022; awaiting next production deploy to https://windyroad.com.au, LinkedIn OG cache flush via Post Inspector, and visual confirmation of the OG preview on a shared homepage link.
- **P010 and P014**: cover the same root cause (OpenAI bot protection + Reddit tool-layer block). P010 tracks the original gap; the OpenAI Google News RSS workaround landed in SKILL.md step 2 on 2026-04-25, narrowing the residual gap to Reddit. Reddit fix is captured in P014 (Playwright helper). P010 stays Known Error and may close when P014 ships. WSJF dropped 12 to 9 reflecting the post-fix scope.
- **P008, P015, P017**: form the substance-gap series. P008 ships option 2 editorial-sim subagent (`wr-newsletter-editor`) per new ADR 020 on 2026-04-25 (verifying); options 1 (rubric expansion 21-31) and 3 (content-risk subagent per ADR 018) already shipped in earlier work. P015 covers drafter-fidelity to capture transcript (Parts 1 + 2 released 2026-04-25 per ADR 019; verifying); P017 covers internal contradictions and referential ambiguity (third-wave rubric expansion 32-38 released 2026-04-25; verifying). All three driven by Tom\'s external editorial review surfacing categories the structural rubric cannot reach in isolation.
- **P012 and P013**: pair on the ship-gate hook surface. P012 is now scoped to CI-status check on push/release; P013 is the voice-tone extension over external-facing text. The teaser-voice-gate part of P013 shipped 2026-04-25 to wr-newsletter SKILL.md step 15.5 (verifying); the hook part stays bundled with P012\'s CLI-level surface to avoid duplicate work.
- **P016 and P018**: form the Friday-pressure cluster. P016 (verification pending as of 2026-04-25) fixes filter logic that drops significant stories. P018 (verification pending as of 2026-04-25) splits the pipeline into prep + finalise phases per ADR 017 to reduce Friday concentration. Verification of P016 fires on the next `/wr-newsletter` run when an aggregator-only candidate is processed; verification of P018 fires on the first edition where prep runs mid-week and finalise ships Friday under one hour.
- **P011**: render-and-verify discipline note added to wr-newsletter SKILL.md step 12 and shared `scripts/render-svg.mjs` helper created (verifying as of 2026-04-25). Verification fires on the next visual-artifact iteration: logo rework, OG image update, or a fresh cover-image generation in step 12.
- **P002**: hero fold-fit SCSS fix landed 2026-04-25 in commit `72c3c2b` (`min-height` 45vh to 100svh, mobile breakpoint 35vh to 100svh, added `padding-bottom: 4rem` to balance the existing top-padding so vertical centring is true, added `@media (max-height: 600px) { min-height: auto; }` short-viewport guard for WCAG 1.4.10 Reflow at 400% zoom per accessibility-lead review). Transitioned Known Error to Verification Pending on 2026-05-11 (AFK iter 2) per ADR-022; awaiting Tom\'s browser verification across desktop / mobile / zoom on next interactive session.
- **P024**: re-opened to known-error 2026-04-27 after attempt 1 verification was NEGATIVE. Attempt 1 (commit `5665454`, 2026-04-26) appended parenthesised matchers `Edit(.claude/**)` / `Write(.claude/**)`; subsequent AFK edits to `.claude/skills/**` and `.claude/agents/**` continued to hit the same `"haven\'t granted it yet"` denial, confirmed live in the very iteration that landed attempt 2 when its own settings edit was denied. Attempt 2 (this iteration) appends colon-form matchers `Edit:.claude/skills/**` / `Write:.claude/skills/**` alongside the existing parenthesised entries (additive A/B test, scope: skills only, agents kept on parenthesised-only baseline as the control surface). Verification triggers on the next AFK iteration that issues an Edit/Write to a `.claude/skills/**` path without falling back to the python3 heredoc. If the colon form also fails, residual hypothesis is a Claude Code core "sensitive file" gate over `.claude/**` and the ticket graduates to upstream-blocked. Composes-with: P025 (the python3 fallback is what bypasses the em-dash hook).
- **P025 and P030**: pair on the em-dash policy enforcement surface. P025 (verifying as of 2026-04-26) ships the PostToolUse:Bash content scan that closes the Bash-path bypass; P030 (open) tracks amending the upstream `wr-itil:work-problems` SKILL.md marker wording so the contractual whitelist line in `.claude/hooks/no-em-dash-bash.sh` can be removed. Composes-with: P024 (closed-as-verifying earlier in this iteration); the `.claude/**` permission entries that allow editing the new hook from inside an AFK subprocess landed in P024\'s commit. Verification of P025 fires on the next Bash-path em-dash write that gets blocked plus the next orchestrator marker-append flow that passes through.
- **P047/P048/P049 (opened 2026-05-02)**: multi-concern split from iter 2 + iter 3 outstanding-questions queue. P047 (wr-risk-scorer assess-release SKILL prose says "Skill tool" but provides Agent-tool parameter): live evidence iter 3 hit "Unknown skill: wr-risk-scorer:pipeline" following the prose verbatim; orchestrator Step 6.5 hit the same on bypass-marker TTL expiry. P048 (manage-problem Step 5 P094 README refresh bypassed on bulk-creation paths): commit 62f58aa created 11 tickets without README staged in same commit; per-session create-gate marker covers all subsequent Writes for the rest of session, so bulk-Write satisfies gate without per-ticket P094 invocation; 5-day drift before iter 2 preflight caught 12 entries. P049 (reconcile-readme.sh:89 section-order assumption): line picks Closed start as Verification Queue end regardless of whether Parked appears first; iter 2 false-positive STALE on P031, workaround was section reorder. All three composing with P045's placement-discipline; P047 tagged for /wr-itil:report-upstream per Tom's Q1 direction. Per P045 discipline, placement (upstream wr-itil/wr-risk-scorer plugins vs local) is the maintainer's call.
