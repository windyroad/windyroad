# Ask Hygiene Trail. 2026-05-12 AFK iter 6

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| (none) | (no AskUserQuestion calls this iter) | n/a | n/a |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

Iter dispatched non-interactively per AFK constraint. Selection was framework-mediated end-to-end. User pacing constraint pre-classified the 6.0-M tier as non-progressable for AFK (P046 upstream-recommendation, P052 needs design, P053 user-answerable taste, P014 explicit manual-confirmation gate), and the 8.0-S tier as upstream-blocked from prior iters. Iter scanned the 4.0/3.0 tier per the user's "remaining progressable queue" enumeration. P029 (4.0, M, work-problems iteration boundary): verified upstream-only (fix path in plugin-only `packages/itil/skills/work-problems/SKILL.md`; reported upstream as windyroad/agent-plugins#83). P023 (3.0, M, architect-gate drift recovery): verified upstream-only (fix path in plugin-cache `architect-gate.sh`; reported as windyroad/agent-plugins#80). P001 (3.0, M, Next.js build hangs locally): Investigation Tasks explicitly require interactive verification (fresh terminal, system restart, local repro), non-progressable AFK. P048 (3.0, M): upstream-blocked (manage-problem plugin). P045 (3.0, M): lever 1 memory file already shipped 2026-05-02; CLAUDE.md half (the other half of lever 1) is local discipline-note work, non-interactively progressable. Substantive prose work over another marker-only append per user pacing guidance ("marker-append pattern is well-exercised"). Worked P045 lever 1 CLAUDE.md half; all decisions framework-mediated; zero AskUserQuestion branches required.

Pipeline-instability observation 1 (queued for `outstanding_questions` per AFK constraint, **second corroboration this loop, strong signal**): iter 5 architect surfaced an advisory that manage-problem SKILL.md cites ADR-014 for "single-commit governance" but on-disk ADR-014 is the Wardley Mapping ADR. Iter 6's independent architect review re-confirmed by direct grep: `docs/decisions/014-wardley-mapping-as-strategic-lens.proposed.md` is the on-disk ADR-014, and `grep -E 'single.commit|one.commit.per' docs/decisions/` returned zero matches. The architect flagged "the SKILL.md drift itself is a separate P032-shaped finding worth capturing as its own ticket (out of scope for this edit)". Citation surface: the SKILL.md rendered preamble I received this iter cites ADR-014 in at least 6 distinct blocks (Step 5 P094 block, Step 6 P094 block, Step 7 P062 block, Step 9e commit step, Step 11 commit step, plus the README-refresh contract). The misattribution is systemic across the skill. Two-iter corroboration plus direct on-disk verification plus independent architect concurrence make this ticket-worthy on user return; queued for ITERATION_SUMMARY.outstanding_questions as a P032-family direction-setting candidate (category: direction, propose a new ticket for the cross-skill ADR-014 to correct-ADR rewrite per the verify-before-asserting discipline). Iter 6's commit message deliberately did NOT cite ADR-014; commit ride-along was per the existing single-commit practice without citation.

Pipeline-instability observation 2: P057 staging trap fired as documented. After `git mv` to `.verifying.md` and `Edit` of the Status field + `## Fix Released` section, the initial `git add` (pre-Edit) staged only the rename; the Edit-tool content change required a second explicit `git add` to ride into the commit. Behaviour matches the SKILL.md's P057 contract verbatim. Not a friction signal; the skill warned, the agent complied. Worth noting that this is the Nth iter in a row to hit this, codification candidate already exists as the documented P057 rule, so no new ticket warranted.

Pipeline-instability observation 3: minor scripting friction on README-history.md append. The naive `cat /tmp/old_line3.txt >> README-history.md; echo "" >> README-history.md` produced no blank-line separator between the prior fragment (P050) and the appended fragment (P051), since the prior fragment's trailing newline ran straight into the new fragment's leading `>`. Recovered by `head -n -2 file > tmp; mv; printf '\n' >> file; cat ... >> file`. The fix took one round-trip. Not a Step 4b candidate (one-off scripting choice, not a recurring pattern); recorded for completeness.

R6 numeric gate (lazy count >=2 across 3 consecutive retros) check via cross-session trend: this iter's lazy count is 0. Combined with iter 4 (lazy count 0) and iter 5 (lazy count 0), the 3-retro window for the 2026-05-12 loop reads 0/0/0. R6 gate does not fire.
