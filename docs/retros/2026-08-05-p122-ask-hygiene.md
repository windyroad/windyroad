# Ask Hygiene Trail: 2026-08-05 (P122 AFK iter)

Retro surface: `claude -p` subprocess invocation of `/wr-retrospective:run-retro` at the close of the P122 iteration of a scope-pinned `/wr-itil:work-problems` loop.

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| (none) | (none) | (n/a) | No `AskUserQuestion` was invoked at any point in this iteration. The loop prompt pins `NEVER call AskUserQuestion`; direction, deviation-approval and one-time-override observations route to `ITERATION_SUMMARY.outstanding_questions` for loop-end batched presentation. |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

## Notes

ID-namespace note: `ADR-043` below is this repo's **local** decision (Bounded editorial remediation loop for editor and skeptic gates, `docs/decisions/043-...proposed.md`), NOT the upstream `wr-retrospective` plugin's own ADR-043. `docs/retros/README.md` instructs readers to resolve bare `ADR-NNN` in this directory against the upstream cache, so the local referent is named explicitly here, as ADR-043's own header note requires on first mention. `ADR-074` is genuinely upstream (the architect plugin's substance-confirm-before-build decision) and has no local counterpart.

The one genuine category-1 direction-setting surface this iteration (local ADR-043's substance, surfaced by the substance-confirm-before-build guard when the settled P122 fix strategy was found to amend its contract) was NOT asked. It is already queued as a standing blocker from the preceding P121 iteration of the same loop, so re-queuing it would duplicate an open question rather than add one. Under the ADR-074 exclusion it would have classified as `direction`, never `lazy`.

R6 numeric gate: not evaluated this iteration. `packages/retrospective/` does not resolve in this repo, so `check-ask-hygiene.sh` is unavailable. Standing gap, already queued.
