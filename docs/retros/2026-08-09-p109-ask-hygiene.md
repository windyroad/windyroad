# Ask Hygiene trail -- 2026-08-09 (P109 AFK iter)

Per ADR-044 framework-resolution boundary; consumed by `check-ask-hygiene.sh` for cross-session trend.

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| (none) | -- | -- | Zero `AskUserQuestion` calls this iteration. The orchestrator's standing constraint forbade it ("NEVER call AskUserQuestion"), and no decision this iteration needed it: the P109-versus-P099 separation was resolved by the counterfactual test on disk, the two gate rejections were resolved by applying their corrections, and the decision not to post the upstream comment was resolved by the framework (an unresolvable link is a factual blocker, and the skill's queue path already covers a post that cannot be made). |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

## Notes

One near-miss worth recording. The queue-versus-post decision on the upstream lifecycle comment was
the only point where an ask was tempting, because it looks like a judgement call. It is not: the
link 404s, which is a fact, and `/wr-itil:update-upstream` already specifies a queue path for a post
that cannot proceed. Asking would have been lazy under ADR-044 -- the framework resolves it.

The correction-signal hook fired on this iteration's prompt (matched `\bDO NOT\b`) and asked for a
ticket-capture offer. False positive: the match came from the orchestrator's standing operational
constraints ("Do NOT push", "Do NOT use ScheduleWakeup"), not from a Tom correction of assistant
behaviour. No correction-capture ticket was warranted, because there was no correction to capture.
The hook's own false-positive shape was captured instead, as P137, which cites this trail as its
occurrence record.
