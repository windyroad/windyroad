# Ask Hygiene trail: 2026-08-08 (P126 AFK iter)

Retro surface: `/wr-retrospective:run-retro` Step 2d, invoked from the P126 iteration of the AFK `/wr-itil:work-problems` loop.

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| (none) | (none) | n/a | No `AskUserQuestion` call was made. The orchestrator's iteration constraints forbid it outright ("NEVER call AskUserQuestion"), routing direction / deviation / override / substance-confirm surfaces to `ITERATION_SUMMARY.outstanding_questions` per the ADR-044 AFK carve-out. |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

## Notes

Three surfaces that would have fired `AskUserQuestion` in an interactive session were resolved without one, correctly:

- **ADR-074 substance-confirm-before-build** ran its predicate over the four ADRs the fix builds on (ADR-006, ADR-021, ADR-034, ADR-045). All returned exit 1 (confirmed), so the guard did not fire and no ask was owed. Had any been unconfirmed, the AFK path is queue-and-continue, not ask.
- **RFC-006 ratification** (`/wr-itil:manage-rfc RFC-006 accepted`) is a genuine substance-confirm and was queued for the user rather than guessed at.
- **Step 4b Stage 2 fix-strategy selection** was silent agent judgement per ADR-044's framework-resolution boundary, as specified.

The R6 numeric gate (lazy count >= 2 across 3 consecutive retros) does not fire: this retro's lazy count is 0.
