# Ask Hygiene: 2026-08-29 (P132 iter)

AFK `/wr-itil:work-problems` iteration working P132. `AskUserQuestion` is
forbidden in this subprocess by the orchestrator's own constraints, so the
count below is structural rather than earned.

Two decisions arose that an interactive session would have surfaced, and both
were queued rather than asked. The story-map item is the I13 propose-fix
gate's own instruction on its no-story-maps branch: record one item for the
maintainer and carry on. The ADR-048 landing question is direction-setting,
since raising an upstream pull request from another repository is not a call
this loop is authorised to make. Neither is lazy: the framework explicitly
declines to resolve the first, and the second is a genuine option choice with
authority resting outside the loop.

Nothing else was asked. The dispositions that could have been asked were
framework-resolved instead: the retraction followed from reading P128 at the
commit in question, and the decision not to park followed from ADR-048 plus
the two open tickets that own the re-selection cost.

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| (none) | | | |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**
