# Ask Hygiene trail: 2026-08-08 (P125 park + upstream-report iteration)

Per Step 2d of `/wr-retrospective:run-retro` (ADR-044 framework-resolution boundary).

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| (none) | | | |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

Zero `AskUserQuestion` calls this iteration. The iteration ran under an explicit
`NEVER call AskUserQuestion` constraint with a queue-to-`outstanding_questions` fallback,
and no decision arose that the framework could not resolve.

The one decision that looked interactive was not. ADR-036 sets a three-condition
structural predicate for the `marketplace-consumer-cannot-edit-cached-plugin` park
classification, and all three held on inspection, so the park was framework-mediated
rather than a judgement to surface. The architect review confirmed the predicate holds
and that the ADR has already litigated the queue-honesty objection the park raises.
The upstream report likewise fired through ADR-024's AFK auto-invoke branch rather
than through a should-I-file question.

Three gates fired and were satisfied without escalation: `wr-risk-scorer:external-comms`
and `wr-voice-tone:external-comms` on the upstream issue body, and
`wr-risk-scorer:pipeline` on each of the three commits (scored 4, 2 and 4, all within
the appetite threshold of 5).

R6 numeric gate: not evaluated. The `wr-retrospective-check-ask-hygiene` shim is not
on `$PATH` in this repo, which is the same consumer-repo shim-resolution family as P130 (a different failure mode: P130 names a repo-relative path and a hard-coded packages glob, this shim does not resolve at all).
The three trail files for 2026-08-08 (P117, P126, P125) all record a lazy count of 0,
so the R6 condition (lazy count of 2 or more across 3 consecutive retros) is not met on
the on-disk evidence regardless.
