# Ask Hygiene 2026-08-05 (P121 iteration)

Session: AFK `/wr-itil:work-problems` iteration on P121 (no gate compares an edition against the prior edition's shape, so pattern breaks and precedent drift reach the reader). Sibling trail file for the same date: `2026-08-05-ask-hygiene.md` (P123 iteration).

**ID namespace warning.** `ADR-044`, `ADR-072`, `ADR-073` and `ADR-074` below are **upstream `@windyroad` plugin** governance IDs from the skill contracts, not this repository's `docs/decisions/` IDs, which stop at 043 and mean different things. `ADR-020`, `ADR-022`, `ADR-032`, `ADR-035`, `ADR-038`, `ADR-041`, `ADR-043` and `JTBD-005` / `JTBD-200` are local.

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| (none) | (none) | n/a | No `AskUserQuestion` calls were made this iteration. |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

The orchestrator forbade `AskUserQuestion` for this iteration and directed all direction-setting substance to `outstanding_questions`, so the count is zero by construction rather than by judgement. Two genuine category-1 direction-setting surfaces were reached and queued rather than asked:

- The substance-confirm-before-build guard fired on ADR-043 (`wr-architect-is-decision-unconfirmed ADR-043 docs/decisions` exits 0; it is the only unconfirmed decision in the repo). The fix routes through its step 15.37, widens its no-op condition and amends its contract, so the build stopped and the substance was queued. Under the guard's own AFK branch this is the correct handling, and it classifies as **direction**, not lazy, had it been asked.
- The JTBD review found that no documented job records the reader benefit the fix would assert. Amending JTBD-005 and JTBD-200 desired outcomes is direction-setting requiring ratification, so it was queued.

One decision was resolved silently that is worth recording as framework-mediated rather than deferred: the I13 propose-fix RFC-trace gate was NOT fired, because ADR-022's corrected Known Error semantics place the fix proposal (and therefore the RFC) after the Known Error transition, and this iteration deliberately stops at Known Error. Auto-creating an RFC scoped against two unresolved direction calls would have produced exactly the mis-scoped auto-RFC that ADR-073's reassessment criterion watches for.

Cross-session trend: the trail files for 2026-07-13, 2026-07-15, 2026-07-27 and 2026-08-05 (P123) each record **Lazy count: 0**. With this one that is five consecutive zeros, so the ADR-044 R6 numeric gate (lazy count >= 2 across 3 consecutive retros) does not fire and no deviation-candidate is queued. `check-ask-hygiene.sh` is not resolvable in this repository (no `packages/retrospective/` tree and no `wr-retrospective-check-ask-hygiene` shim on `$PATH`), so the trend was read directly from the trail files.
