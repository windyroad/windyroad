# Ask Hygiene trail: 2026-08-09 P129 iteration

Per ADR-044 (Decision-Delegation Contract, framework-resolution boundary), Step 2d of `/wr-retrospective:run-retro`.

Scope: the AFK `/wr-itil:work-problems` iteration that fixed P129 (sourcing a repo script to probe a helper ran its whole flow) and captured P134.

## Calls

| Call # | Header | Classification | Citation |
|--------|--------|----------------|----------|
| (none) | (none) | (none) | No `AskUserQuestion` call was made in this iteration. |

**Lazy count: 0**
**Direction count: 0**
**Override count: 0**
**Silent-framework count: 0**
**Taste count: 0**
**Correction-followup count: 0**

## Notes

The zero is not evidence of good ask hygiene on its own: the iteration ran under an explicit constraint that forbade `AskUserQuestion` outright and directed all user-bound items to `ITERATION_SUMMARY.outstanding_questions`. Read this row as "the constraint held", not as "the framework resolved everything it was asked".

Two decisions in the iteration would have been genuine direction-setting asks (ADR-044 category 1) had the surface been available, and both were routed to `outstanding_questions` instead:

1. Whether to author a new job covering unattended-action safety. The JTBD reviewer found that no documented job covers it and drafted a candidate (proposed JTBD-403). It was deliberately NOT authored: new jobs are direction-setting and need human confirmation exactly as a new ADR does, and creating a fourth unratified artefact while the only person who can ratify is asleep would have made the corpus worse. Queued with the job statement in plain words so it can be answered without opening the repo.
2. Whether P129 should sit at Known Error or advance further. This one turned out to be framework-resolved rather than direction-setting: `/wr-itil:transition-problem` Step 3 permits one hop at a time from `.open.md`, and the Known Error to Verification Pending hop requires a released fix, which an iteration forbidden to push cannot produce. Recorded here because it was initially misread as a judgement call; the framework did settle it, so asking would have been lazy.

Both governance reviewers (architect, jtbd) and the risk scorer were dispatched synchronously as subagents, which is delegation rather than an ask and is not counted here.

R6 numeric gate (lazy count >= 2 across 3 consecutive retros): cannot fire on this retro, lazy count is 0.
