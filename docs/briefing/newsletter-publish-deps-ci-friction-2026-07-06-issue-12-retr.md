## Newsletter publish + deps/CI friction (2026-07-06 Issue 12 retro)

- **A routine content push can be blocked by the deps-hygiene chain, and clearing it is unrelated dependency work (P026 family).** On publish day, `push:watch`'s dry-aged-deps auto-bumped `@types/react` in package.json but NOT package-lock.json (out of sync breaks `npm ci`); `fix:deps`' green-check then failed because `vitest run` globbed a standalone Playwright e2e script (`tests/mobile-nav.spec.mjs`, bare `async function test()` driving localhost:3000, no vitest import) as a unit suite ("No test suite found in file"). After fixing that (commit 7fe5046, added `tests/**/*.spec.mjs` to `vitest.config.ts` `test.exclude`), the push landed but remote CI "Check dependency freshness" failed while LOCAL dry-aged-deps reported no safe/mature updates: a local-vs-CI freshness-gate divergence that leaves master red on ambient staleness (transitive vulns needing parent-bumps/overrides that neither auto-resolve nor `fix:deps` applies). NB: a `vitest.config.ts` (react/jsdom/setup/`@`-alias) already exists; never add a second `vitest.config.mjs`, it shadows the `.ts` and drops jsdom/setup/alias (architect caught this). Edit the existing config.
- **Commit-message risk gates hash the EXACT message; shell-escaped backticks/quotes in `git commit -m` diverge from what you passed `wr-risk-scorer:external-comms`, causing a false "not reviewed" block.** Keep commit messages plain (no backticks or double-quotes) so the gate's hash matches the reviewed text. Cost me a re-run this session.
- **The architect/JTBD gate-marker TTL (3600s) expires repeatedly across a long newsletter gate-round session (~8 refreshes this session).** A prep+finalise run with many subagent review rounds (each minutes long) routinely blows the 1-hour TTL between marker-set and the next Write, forcing an architect+jtbd re-run just to refresh. Batch project-file writes close to a fresh marker, or expect the refresh tax.
- **The drafter reinserts the banned word "actually" reflexively across author-directed rewrites (voice gate caught it three separate times this session).** After each correction re-draft, re-scan for the voice guide's banned words before resubmitting; "actually" is the repeat offender.
- **`git mv` fails on untracked files ("source directory is empty"); use plain `mv` to move a never-committed draft dir into the published tree, then `git add`.** The whole `drafts/leader/<date>/` edition dir is untracked until first commit.

