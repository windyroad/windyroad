## Governance-iteration friction (2026-08-05 P121 AFK iter)

- **ADR-043 is the only unconfirmed decision in `docs/decisions/`, and it gates every newsletter-gate ticket built on the remediation loop.** `wr-architect-is-decision-unconfirmed ADR-043 docs/decisions` exits 0. P121, P113 and P122 all route through its step 15.37, so the substance-confirm-before-build guard fires on each of them and the build stops at Known Error until Tom ratifies. Run `/wr-architect:review-decisions` before planning any of that work, or the iteration spends its budget on an investigation it cannot ship.
- **Check WHEN a gate runs before arguing about WHAT axis it should own.** P121 was framed as a coverage-partitioning question (extend the cross-edition gate or add a sibling). The pipeline's own step order settled it in one read: the ADR-038 gate fires at SKILL.md step 11.4 and the LinkedIn post is not drafted until 15.5, so that gate structurally cannot see the surface carrying most of the evidence. An ordering fact beats an architectural preference argument, and it is cheaper to check.
- **The published newsletter corpus is post-remediation and is useless as a regression fixture.** `src/newsletters/published/leader/2026-08-03/` carries the issue line, `This week:`, the reply instruction and the restored forward-deadline slot, because Tom's reviewer had them fixed before publish. A fixture built from it finds nothing. The pre-remediation state is in git history under `src/newsletters/drafts/leader/<date>/`: for Issue 16, commit `d2d674a` is the pipeline's own declared publication-ready draft and reproduces all five shape breaks. Always take newsletter fixtures from the drafts folder's history, not the published tree.
- **Nothing recomputes WSJF at a status transition, and the consistency checks cannot catch it.** Transitioning Open to Known Error changes the status multiplier from 1.0 to 2.0, but the Step 7 pre-flight only mandates an Effort re-rate, the README refresh is "a render, not a re-rank", and `reconcile-readme` only compares the ticket body against the README row, so a value wrong in both passes clean. It cost a round on P121 and is now P125. Recompute `(Severity x multiplier) / divisor` by hand on every transition. Watch for the coincidence: Effort M to L doubles the divisor while the multiplier doubles too, so the correct value can be unchanged, which does not look like an error either.
- **The commit-gate risk scorer catches things the four review gates structurally cannot.** Architect, jtbd, style-guide and voice-tone all passed the P121 transition; the scorer caught a wrong WSJF number none of them reads. It also verified the architect's own on-disk citations independently. Treat it as a real reviewer with a different lens, not a formality at the end.
- **The external-root-cause detector in `manage-problem`'s transition step fires on the word "external" wherever it appears, including "external reviewer" describing a person.** Any ticket discussing external review trips it. The escape hatch is designed in: append the option-3 marker `- **Upstream report pending** -- false positive; detection misfire` to `## Related` with the reason, and the already-noted check skips it on later runs. Do not let the AFK auto-invoke branch file an upstream issue against nobody. (The detector is an upstream `wr-itil` surface, tracked under an upstream ticket ID that collides with this repo's own P063, which is an unrelated closed newsletter-cover ticket. Cite the mechanism, not the number.)

