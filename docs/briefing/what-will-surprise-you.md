## What Will Surprise You

> Older entries rotate to [`what-will-surprise-you-archive.md`](what-will-surprise-you-archive.md) under the Tier 3 budget; nothing has ever been deleted. Rotation here is by signal rather than age. The archive tail is deliberately NOT split further even though it is past the 2x must-split ratio: nothing loads an archive at session start, so another generation behind the `-archive-early` siblings would be churn. That decision and its measurements are recorded on P150; do not re-litigate it from the byte counts alone. Most recent pass: 2026-08-29 (P099 iter), which added the `SendMessage`-marker entry below and rotated the `check-briefing-budgets` detector entry out when that addition took the file over the ceiling. That entry was joint-lowest of the three at signal-score 2 on this file's own signal-not-age rule, and the tiebreak against the other 2 was that it is explicitly true-at-a-version and its own byte figures had already been falsified three times by the edits carrying them. The scores that pass quotes are its own; the P165 iteration later that day re-scored both survivors to 3, which does not disturb the choice it made.

- **Two reviewers converging independently is the strongest signal this stack produces, and it means stop, not iterate.** On 2026-08-23 the architect and the JTBD reviewer, given different prompts and reading different corpora, independently killed the same drafted `.github/dependabot.yml`: the architect on the ground that it reinstated a bot authoring path whose acceptance died with a supersession, the JTBD reviewer on the ground that it routed a code-quality judgement to a persona whose file explicitly excludes it. Neither had seen the other's verdict. When that happens the answer is not a better version of the artefact, it is that the artefact is the wrong shape. Dropping it and recording the open question took one round; arguing with either reviewer alone would have taken several and landed the same place. Third observation of this pattern, after the 2026-08-09 P109 iteration and the 2026-08-23 P141 iteration. **A fourth on 2026-08-23 extends it to three reviewers and to a case where the artefact was right and one line of it was wrong**: on the P154 fix the architect, the JTBD reviewer and the voice reviewer each independently found that a new lint check stripped every parenthesised span while its own header claimed it stripped only link URLs. None had seen the others. So convergence does not always mean stop; it means the finding is real and the only question left is which surface moves. There the code moved to match the header, because the header was describing the behaviour anyone would want.
  <!-- signal-score: 3 | last-classified: 2026-08-29 | first-written: 2026-08-23 -->

- **Resolving a governance reviewer's findings by `SendMessage` is the right review move and the wrong marker move, and the roll-up's "dispatch a fresh synchronous Agent spawn" does not read as a warning about it.** The Critical Points entry on the six gates is correct and was in context on 2026-08-29 during the P099 iteration; the trap was still walked into, which is what makes this worth its own entry. The pull is real: when the architect returns ISSUES FOUND, resuming that agent is genuinely better review practice, because it keeps the context that produced the findings and lets the reviewer check that the resolution actually answers them. Both the architect and the JTBD reviewer were resumed that way, both upgraded to PASS on the merits, and both edits stayed blocked, because a `PostToolUse` hook matching the literal string `Agent` never fired. Recovery cost two full re-spawns of reviews already passed. The shape that gets both things: iterate by `SendMessage` as much as you like, then make the FINAL pass a fresh synchronous `Agent` call carrying the settled design. That final spawn is not wasted ceremony, since it re-reads the artefact from disk and is the one that writes the marker. Only `wr-architect` names the trap, in its deny string, and the JTBD gate is the one likelier to be hit first because its scope is every project file. Fourth recorded witness; the mechanism and the other three are on P124.
  <!-- signal-score: 3 | last-classified: 2026-08-29 | first-written: 2026-08-29 -->

