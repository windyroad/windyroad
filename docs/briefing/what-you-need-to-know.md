## What You Need to Know

> Older entries rotated to [`what-you-need-to-know-archive.md`](what-you-need-to-know-archive.md) on 2026-08-08 and again on 2026-08-09 (Tier 3 budget). The 2026-08-09 pass rotated by signal, moving five entries that were not exercised in that iteration. Nothing was deleted.

- **Verify a ticket's own premises before acting on them, not just its prose.** P099's Fix Strategy asserted this repo had no `docs/rfcs/` tier and treated the I13 gate firing as a known false positive. Five RFCs existed. P108 pinned a plugin path at a version no longer on disk. Both were stale claims inside otherwise-current tickets, and both would have produced wrong action if taken at face value. Extension observed on the P117 close: the ticket carried its OWN counter-evidence to its own closure argument, in a section written three days after the one that argued for keeping it open. Read the whole ticket before believing any one section of it.
  <!-- signal-score: 6 | last-classified: 2026-08-09 | first-written: 2026-08-08 -->
- **Parking an upstream-blocked ticket is structural here, not a judgement call.** ADR-036 sets a three-condition predicate: the investigation names a specific fix site, that site lives under `~/.claude/plugins/cache/<vendor>/<plugin>/<version>/`, and this project carries no `packages/<plugin>/` source tree. When all three hold the ticket parks regardless of how high its WSJF is or whether a local workaround exists, and the ADR has already litigated the queue-honesty objection: leaving it ranked means the orchestrator re-selects work it cannot action every loop, which is P096. The park is a five-step workflow, not a rename: verify the bug on the LATEST cached version, search for an existing upstream issue, file one and record it under `## Reported Upstream` BEFORE the transition, add a `## Parked` section, then move the README row from WSJF Rankings to Parked in ID-sort position. Two things are easy to miss. Parked carries WSJF multiplier 0, so the ticket's own WSJF line must be recomputed as part of the transition. And 15 of this repo's 16 parks are now this same classification, which is ADR-036's own reassessment trigger.
  <!-- signal-score: -1 | last-classified: 2026-08-09 | first-written: 2026-08-08 -->
- **Two of run-retro's own detectors cannot run here, and one of them fails loudly enough to look like your mistake.** `wr-retrospective-check-readme-jtbd-currency` exits with `check-readme-jtbd-currency: packages dir not found: packages`, and the Tier-3 budget pass names `packages/retrospective/scripts/check-briefing-budgets.sh`, which does not exist in a repo that installs the plugin from the cache. Both reproduce on every retro; this is P130. Neither is your setup. The README-currency answer here is genuinely "nothing to check", and the budget pass has a one-line substitute that gives the rotation decision its evidence: `for f in docs/briefing/*.md; do printf "%s %s\n" "$(wc -c < "$f")" "$f"; done | sort -rn`, read against the 5120-byte ceiling and the 10240-byte MUST_SPLIT ratio. `wr-retrospective-check-autocreate-rfc-scope` and `wr-retrospective-measure-context-budget` both work; it is only these two.
  <!-- signal-score: 3 | last-classified: 2026-08-09 | first-written: 2026-08-08 -->

- **The maintainer persona exists and is ratified (2026-08-09), so internal-tooling work is reachable AFK again.** `docs/jtbd/` covered only audience personas for months, so editing internal tooling gave the jtbd edit-gate nothing to map to, and four tickets captured on 2026-08-08 (P130, P131, P132, P133) had to record their anchoring as explicitly unconfirmed. Tom's 2026-06-17 direction to author an internal-maintainer persona locally was acted on 2026-08-08: `docs/jtbd/internal-maintainer/` holds the persona plus JTBD-400 (trust what the loop did while I was away), JTBD-401 (decide from the phone in my hand) and JTBD-402 (land the fix where the defect lives), in a new 400-499 ID band. Tom read all four in session on 2026-08-09; every one now carries `human-oversight: confirmed`, `oversight-date: 2026-08-09`. Anchor internal-tooling tickets to these jobs in prose under `## Related`, not in header lines, and no longer add a provisional caveat. Tickets written before 2026-08-09 still carry that caveat in their own prose. Read it as stale, not as current state. What the ratification unblocked, and why it mattered: while the persona was unconfirmed, the JTBD edit gate withheld its marker on ISSUES FOUND, and "reasons from an unratified persona" was an ISSUES FOUND an agent could not clear, because ratifying is Tom's and an AFK iteration cannot ask. That blocked a fully designed, twice-reviewed fix on the P115 iteration after three JTBD rounds. Prose always landed; it was executable enforcement that was gated, and it no longer is.
  <!-- signal-score: 4 | last-classified: 2026-08-09 | first-written: 2026-08-08 -->
