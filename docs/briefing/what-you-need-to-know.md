## What You Need to Know

> Older entries rotated to [`what-you-need-to-know-archive.md`](what-you-need-to-know-archive.md) on 2026-08-08 and again on 2026-08-09 (Tier 3 budget). The 2026-08-09 pass rotated by signal, moving five entries that were not exercised in that iteration. Nothing was deleted.

- **Verify a ticket's own premises before acting on them, not just its prose.** P099's Fix Strategy asserted this repo had no `docs/rfcs/` tier and treated the I13 gate firing as a known false positive. Five RFCs existed. P108 pinned a plugin path at a version no longer on disk. Both were stale claims inside otherwise-current tickets, and both would have produced wrong action if taken at face value. Read the whole ticket before believing any one section of it: on the P117 close it carried its own counter-evidence in a section written three days after the one that argued the other way.
  <!-- signal-score: 7 | last-classified: 2026-08-23 | first-written: 2026-08-08 -->
- **Two of run-retro's own detectors cannot run here, and one of them fails loudly enough to look like your mistake.** `wr-retrospective-check-readme-jtbd-currency` exits with `check-readme-jtbd-currency: packages dir not found: packages`, and the Tier-3 budget pass names `packages/retrospective/scripts/check-briefing-budgets.sh`, which does not exist in a repo that installs the plugin from the cache. Both reproduce on every retro; this is P130. Neither is your setup. The README-currency answer here is genuinely "nothing to check", and the budget pass has a one-line substitute that gives the rotation decision its evidence: `for f in docs/briefing/*.md; do printf "%s %s\n" "$(wc -c < "$f")" "$f"; done | sort -rn`, read against the 5120-byte ceiling and the 10240-byte MUST_SPLIT ratio. `wr-retrospective-check-autocreate-rfc-scope` and `wr-retrospective-measure-context-budget` both work; it is only these two.
  <!-- signal-score: 4 | last-classified: 2026-08-23 | first-written: 2026-08-08 -->

- **The maintainer persona exists and is ratified (2026-08-09), so internal-tooling work is reachable AFK again.** `docs/jtbd/` covered only audience personas for months, so editing internal tooling gave the jtbd edit-gate nothing to map to, and four tickets captured on 2026-08-08 (P130, P131, P132, P133) had to record their anchoring as explicitly unconfirmed. Tom's 2026-06-17 direction to author an internal-maintainer persona locally was acted on 2026-08-08: `docs/jtbd/internal-maintainer/` holds the persona plus JTBD-400 (trust what the loop did while I was away), JTBD-401 (decide from the phone in my hand) and JTBD-402 (land the fix where the defect lives), in a new 400-499 ID band. Tom read all four in session on 2026-08-09; every one now carries `human-oversight: confirmed`, `oversight-date: 2026-08-09`. Anchor internal-tooling tickets to these jobs in prose under `## Related`, not in header lines, and no longer add a provisional caveat. Tickets written before 2026-08-09 still carry that caveat in their own prose. Read it as stale, not as current state. The history of what the ratification unblocked is settled and now lives in [`governance-iteration-friction-2026-08-09-p115-iter.md`](governance-iteration-friction-2026-08-09-p115-iter.md).
  <!-- signal-score: 5 | last-classified: 2026-08-23 | first-written: 2026-08-08 -->
- **The story-map gate now hard-refuses in this repo, and it is not the RFC-trace failure older notes describe.** `wr-itil-check-fix-rfc-trace` used to ask for an RFC; on the version this repo runs it asks for a release row on a story map, and because `docs/story-maps/` does not exist it exits **3** rather than reporting an untraced problem. Exit 3 means the predicate refuses to answer, not that your ticket failed a check, and its own directive says to record one item for the maintainer and carry on rather than stopping. The local precedent set when P070 closed still applies and was applied again on 2026-08-23 for P141: when the fix's design already lives in a decision record, land it and record the gate decision in the ticket, rather than bootstrapping a whole tier for one small edit. Do not read exit 3 as a blocker. One thing the exit-3 path hides: the predicate answers from a union of two tiers and scans `docs/rfcs/` FIRST, so a problem an RFC already traces exits 0 before the map count is ever consulted. This repo has an RFC tier, so expect either outcome depending on the ticket.
  <!-- signal-score: 2 | last-classified: 2026-08-23 | first-written: 2026-08-23 -->
- **Regenerating the decisions compendium fights this repo's no-em-dash hook every single time.** `wr-architect-generate-decisions-compendium` IS on `$PATH` (check with `command -v`, not by globbing the repo; several plugin-cache versions ship it, so do not cite one), it works, and it writes 60-odd em-dashes into `docs/decisions/README.md`, which `.claude/hooks/no-em-dash-bash.sh` then blocks on the PostToolUse scan. The committed copy has zero, so every prior session normalised them by hand too. Run the generator, then convert with a one-liner replacing the spaced form first and the bare form second, then commit. Budget one extra step for this whenever you add an ADR; it is not a broken install and it is not your edit that introduced them.
  <!-- signal-score: 2 | last-classified: 2026-08-23 | first-written: 2026-08-23 -->
