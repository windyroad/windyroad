## Pipeline friction (2026-06-17 P079 ADR-026 amend iter)

- **Do NOT run `wr-architect-generate-decisions-compendium` to refresh `docs/decisions/README.md` after amending an ADR body (P087).** The script is deprecated under ADR-078 (the compendium is now auto-authored by the `architect-compendium-update-entry.sh` PostToolUse hook on every ADR Edit), AND its full-regen quotes pre-P025 ADR bodies verbatim, reintroducing em-dashes (43 in one run on 2026-06-17) that the `no-em-dash-bash.sh` PostToolUse hook then blocks. The architect agent's stock recommendation still says "regen via the ADR tooling", which points at this deprecated, hook-blocked path. Correct path for a one-entry ADR amendment: let the PostToolUse hook re-author the single entry on Edit, OR refresh just that ADR's `### ADR-NNN` block by hand in the existing generator format (note the committed README uses the generator's `**Chosen:**` field shape, while the hook emits `**Decides:**` -- re-triggering the hook on a generator-format file mixes the two). The `architect-readme-pairing-check.sh` pre-commit gate still requires `docs/decisions/README.md` to be staged alongside any staged ADR body change, so a paired README edit (or `RISK_BYPASS: architect-compendium-deferred`) is mandatory.

