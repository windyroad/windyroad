## Newsletter pipeline gates vs structural defects (2026-06-15 Issue 09 retro)

- **The five newsletter gates (voice, content-risk, critic, editor, cog-a11y) + cross-edition + URL-verify can all PASS on a draft that still has structural and sourcing defects.** Issue 09 passed every gate first-time yet Tom flagged 8 defects: redundant Source lines when inline links exist, outlets named without linking, over-proving a first-hand/widely-known story, dropped "Also worth noting" section, missing "Issue NN:" H1 prefix, missing CTA horizontal rule, inconsistent model naming across brief vs LinkedIn, and sparse dated citations. The gates are LLM-judgement reviewers; none checks deterministic format invariants. Captured as P089. **Fix shipped 2026-06-15 (commit a256322, now Verifying):** `scripts/check-newsletter-structure.sh` deterministically checks seven invariants (no redundant Source line; no outlet named without a link [check (b), see below]; `### Also worth noting` present; H1 `Issue NN:` prefix; pre-CTA `---` rule; cross-file model-name consistency; no CTA services-pitch [check (g), P090]); wired into `/wr-newsletter` step 16 as a blocking pre-save check; invariants codified in step 11b + `assets/draft-template.md`. "Rubric PASS does not mean the artefact is good" recurs on the structural axis; the deterministic lint is the structural-axis answer the LLM gates could not provide. **Check (b) tightened 2026-06-16 (commit dabff77, P093 closed):** it now flags a link-free line naming even a SINGLE bare outlet (previously only 2+), per Tom's pipeline rule "do not name a news site without linking it". A per-item carve-out lets a legitimate same-item back-reference ("the WSJ piece is worth reading") pass when that outlet is linked elsewhere in the same `### ` item, matched by outlet name, canonical domain, or the finance.yahoo.com->Bloomberg / yahoo.com/news->Axios syndication map. Practically: name an outlet on a link-free line and it must be linked somewhere in that same item or the lint fails.
- **The newsletter CTA must NOT carry the Windy Road services pitch.** The CTA is the rotating reader-invitation + linked windyroad.com.au only. The leader persona-config "Description variants" are the recurring source of the pitch; the drafter pulls one every edition and Tom removes it every edition ("I've said that before"). Captured as P090.
- **When the pipeline cannot resolve a source URL, ASK Tom rather than dropping the outlet name or naming-without-link.** Google News RSS only exposes bare publisher domains; paywalled outlets (WSJ/Bloomberg/NYT/Axios) 403 on curl. Tom is willing to supply canonical URLs on request ("If you cannot resolve a URL, ask me"). Do not silently degrade attribution. Captured as P091.
- **push:watch is hostile to the amend-to-fix-a-just-pushed-commit workflow.** Its auto pull-rebase replays a sibling-amend onto origin and injects conflict markers into working files mid-session. Recover via `git rebase --abort` + `git push --force-with-lease`. push:watch also false-fails ("Pipeline failed") on transient GitHub API network errors (connection reset, and a freshly-observed HTTP 401 "Bad credentials" blip on run 27609565746) when CI actually succeeded; verify the real conclusion via `gh run view <id> --json conclusion` before believing the failure. **Fix committed 2026-06-16 (commit adbdb72, P092 now Known Error awaiting orchestrator push):** `scripts/push-watch.sh` adds `is_sibling_amend()` (skips the auto-rebase on a same-parent divergence, warns, suggests `--force-with-lease`) and `watch_run_resilient()` + `is_transient_gh_error()` (re-checks the real CI conclusion via `gh run view` with bounded retry, declaring failure only on a genuine conclusion=failure; the transient set includes HTTP 401 "Bad credentials"). Behavioural test at `scripts/push-watch.test.mjs` (8 cases) via the `PUSH_WATCH_LIB_ONLY` source seam. The manual recovery/verify workarounds above remain useful only until the fix ships.
- **Pre-set the commit-gate markers before committing to avoid the P085 discard loop.** The external-comms commit gate discards working-tree edits when it blocks, and re-staging changes the marker hash, looping. Run `wr-risk-scorer:pipeline` + `wr-risk-scorer:external-comms` + `wr-voice-tone:external-comms` on the exact final commit message BEFORE the first `git commit` so the commit never blocks.

