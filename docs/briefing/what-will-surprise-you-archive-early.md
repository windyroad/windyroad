# What Will Surprise You (early archive)

> Rotated 2026-08-08 (second pass) from `what-will-surprise-you-archive.md`, which was 14650 bytes against the 5120-byte Tier 3 ceiling and past the 2x must-split ratio. Split by position again, the same age proxy the first pass used: these entries are the older half. Nothing was deleted.

- **Tom's editorial reviews repeatedly catch weaknesses the critic subagent does not, and as of Issue 16 we know WHY: the gates that catch them are non-blocking by design.** The `wr-newsletter-editor` (ADR-020) and `wr-newsletter-skeptic` (ADR-042) gates are single-shot and surface-to-Tom. ADR-020 line 41 rejected an iterating editor and ADR-042 inherited the same "does not auto-rewrite; Tom decides" contract. On Issue 16 the editor ran ONE round, returned NEEDS_EDITORIAL_REVISION, and named three defects (fold-compression, atwn-thesis-fit on two ATWN notes, audience-pointer-specificity) that Tom's external reviewer then re-derived independently after publication-readiness. The gates are not missing these; the pipeline routes their findings to Tom. Meanwhile every gate that HAS a remediation loop (voice, content-risk, cross-edition) converged without him. ADR-020 pre-registered its own reversal: "If retrospective evidence shows iteration would help, a follow-up ADR can lift the loop." P120 carries that evidence.
- **The critic subagent defined at `.claude/agents/wr-sw-critic.md` will not be discoverable in the session it was created in.** Work around with `general-purpose` plus the critic process inlined. See problem 007.
- **The `@windyroad` plugins can be silently disabled by an uncommitted `.claude/settings.json` edit, which degrades the newsletter pipeline.** On the 2026-05-25 session the `enabledPlugins` block had been stripped to just `skill-creator`; the `wr-voice-tone` gate was unavailable and the run fell back to a `general-purpose` subagent reading `docs/VOICE-AND-TONE.md` directly (it worked, but missed the dedicated agent's stricter reader-respect checks). Reverting `settings.json` does NOT restore plugins mid-session: plugin/agent registration happens at session start, so a restart is required before the agents reappear. Sibling to the new-agent-discoverability point in "What You Need to Know".
- **Content-risk review now runs as a fresh-context subagent** via `wr-content-risk-scorer` (ADR 018, lands 2026-04-25). Same five-axis output format as the inline path (pinned by ADR 015 confirmation criterion 3); the only change is the execution context shifts from inline to fresh subagent. See ticket 009 (verification pending; close on next live `/wr-newsletter` exercise).
- **OpenAI's news page returns 403 to `WebFetch`** and 401 to `curl`-with-browser-UA (Cloudflare challenge). Proven workaround this session: Playwright via `node -e "import('playwright')..."` with Chromium UA passes the challenge and returns 200; openai.com slug convention is `openai.com/index/<slug>/` and slugs follow the post title closely (verify-and-iterate works). Google News RSS scoped `site:openai.com` is a lighter-weight fallback for titles only. Reuters is 401-blocked even via Playwright; no workaround found this session. Reddit is blocked at the Claude Code tool layer entirely. See problems 010 and 014.
- **Visual artifacts (SVG logos) need render-and-verify discipline**. `sips -s format png -Z <size> in.svg --out out.png` + `Read` on the PNG. Tom had to direct this explicitly after multiple iterations shipped unverified. See problem 011.
- **The Wardley map is internal-only substrate**. Never named, never referenced in body copy. The reader sees the analysis that results from the map, not the map vocabulary (no "map", "landscape", "position", "evolution axis", "commodity", "genesis" in reader-facing prose).
- **Map anchor is a preference, not a hard filter.** Three-lens-strong items without a clean map mapping still qualify. The original "every item must anchor" rule was softened.
- **Tier-1 source rule is a heuristic, not a hard block.** If one tier-1 source fails but tier-2 coverage is rich, judgement says proceed with the map update and note the partial coverage. Blind rule-following produced a bad call earlier in the session.
- **Publication-day workflow is multi-hour under current pipeline.** The `/wr-newsletter` skill runs end-to-end in one sitting (sources, map, critic loops, image, LinkedIn post, publish). Friday-morning sessions regularly take 2 to 4 hours. Phase split into prep (Mon to Thu) plus finalise (Fri) is planned. See P018.
- **The filter step drops significant stories on attribution heuristic without attempting corroboration.** The Apple-CEO transition was missed on this pattern in the 2026-04-24 edition; multi-outlet corroboration via Google News was trivially available. Filter-step corroboration branch planned. See P016.
- **The critic rubric did not catch internal contradictions, referential ambiguity, jargon density, voice drift, scaffolded headlines, news-peg-first H1s, or unsupported author commitments.** P017 third-wave rubric expansion shipped 2026-04-25 (verifying): newsletter-critic-rubric.md grew from 31 to 38 checks (added check_32 internal-consistency, check_33 referential-clarity, check_34 density / consultant-speak, check_35 voice consistency across sections, check_36 item-headline distinctiveness, check_37 headline thesis-first, check_38 author-commitment honor-ability). Wardley critic mirrored check_32 and check_33 at check_17 and check_18 (16 to 18 checks). Verification fires on the next `/wr-newsletter` run. **Superseded 2026-06-16 (P075):** the numbered checks no longer exist. `newsletter-critic-rubric.md` was rewritten to the ADR-035 check-free editorial-reader format (STRENGTHS / WEAKNESSES / optional RELEVANT CONTEXT, no numbered checks, no scoring table). Tickets and prior briefing entries that cite `check_NN` are stale references to that pre-ADR-035 shape. Heading-craft coverage (the old check_36 item-headline-distinctiveness and check_37 headline-thesis-first) was re-homed by P075 as the rubric's prose "Headline craft" section (headline clarity, jargon density, sameness-across-headings) plus drafter step 11a/11b discipline (discourage the colon-flourish default, gloss-on-first-use at heading granularity). Outstanding upstream sub-task: cross-reference the `wr-voice-tone` plugin agent prompt to audit gloss-on-first-use at heading granularity (not editable from this consumer repo).
- **Tier-1 source list misses US regulatory-news outlets** (Politico, Axios, Reuters technology, NYT technology, Washington Post technology). The Trump White House CAISI / pre-release-vetting story almost shipped missing on the 2026-05-08 edition; Tom recalled it externally. None of the SKILL.md tier-1, tier-2, or tier-3 sources would have surfaced it. Captured as P051; re-rate at next `/wr-itil:review-problems`.
- **Cog-a11y is now a numbered gate (SKILL.md step 15.4 / 15.4-prime, P053).** It runs after the editor as a one-round pass with optional remediation (target Grade 10 leader, 11 developer). Exercised on the 2026-05-25 Issue 06 finalise: returned NEEDS_REVISION_OPTIONAL at Grade 11.3, brought to ~Grade 10 by splitting the longest multi-clause sentences and expanding "SDK". The codification this entry previously flagged as a candidate has landed. **The Grade 10 leader target is a deliberate, accepted setting (Tom decision 2026-06-15, P065 closed 2026-06-16):** the leader register structurally scores Grade 11+ on compound-noun density, and the recurring NEEDS_REVISION_OPTIONAL advisory verdict is EXPECTED, not a calibration bug. Apply the high-impact splits + abbreviation expansions each edition, skip the in-group-jargon flags that conflict with leader-register precision ("tier-1 lab", "frontier model"), and surface residuals in the reviews companion file. Do NOT re-open the Grade-target question or raise the target to Grade 11 - Tom chose accept-current-behaviour over both the raise-target and exemption-list options; no persona-frontmatter override, no ADR.
- **LinkedIn comment replies are a new artefact type sibling to the brief body.** The 2026-05-08 edition produced `<draft-folder>/<date>.reply-rustvm-attack-mapping.md` (a reader question on AISI's challenge design and MITRE ATT&CK mapping). Architect noted: one file per session is below the trigger threshold; two or three across consecutive editions warrants amending ADR-026's sibling-file pattern to include `.reply-*.md`. Track the recurrence.
